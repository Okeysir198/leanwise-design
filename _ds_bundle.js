/* @ds-bundle: {"format":4,"namespace":"LeanWiseDesign_f2d907","components":[{"name":"AgentTrace","sourcePath":"components/ai/AgentTrace.jsx"},{"name":"Artifact","sourcePath":"components/ai/Artifact.jsx"},{"name":"ConfidenceMeter","sourcePath":"components/ai/ConfidenceMeter.jsx"},{"name":"DiffReview","sourcePath":"components/ai/DiffReview.jsx"},{"name":"Feedback","sourcePath":"components/ai/Feedback.jsx"},{"name":"Message","sourcePath":"components/ai/Message.jsx"},{"name":"PromptInput","sourcePath":"components/ai/PromptInput.jsx"},{"name":"SourceChip","sourcePath":"components/ai/SourceChip.jsx"},{"name":"SourceList","sourcePath":"components/ai/SourceList.jsx"},{"name":"ToolCall","sourcePath":"components/ai/ToolCall.jsx"},{"name":"ActivityFeed","sourcePath":"components/data/ActivityFeed.jsx"},{"name":"BarChart","sourcePath":"components/data/BarChart.jsx"},{"name":"CodeBlock","sourcePath":"components/data/CodeBlock.jsx"},{"name":"Console","sourcePath":"components/data/Console.jsx"},{"name":"DataGrid","sourcePath":"components/data/DataGrid.jsx"},{"name":"EmptyState","sourcePath":"components/data/EmptyState.jsx"},{"name":"FilterBar","sourcePath":"components/data/FilterBar.jsx"},{"name":"Toolbar","sourcePath":"components/data/FilterBar.jsx"},{"name":"KpiTile","sourcePath":"components/data/KpiTile.jsx"},{"name":"LineChart","sourcePath":"components/data/LineChart.jsx"},{"name":"Pagination","sourcePath":"components/data/Pagination.jsx"},{"name":"Progress","sourcePath":"components/data/Progress.jsx"},{"name":"StatMeter","sourcePath":"components/data/StatMeter.jsx"},{"name":"StateView","sourcePath":"components/data/StateView.jsx"},{"name":"Table","sourcePath":"components/data/Table.jsx"},{"name":"SERIES","sourcePath":"components/data/chart-parts.jsx"},{"name":"DataTable","sourcePath":"components/data/chart-parts.jsx"},{"name":"Legend","sourcePath":"components/data/chart-parts.jsx"},{"name":"Calendar","sourcePath":"components/forms/Calendar.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"Combobox","sourcePath":"components/forms/Combobox.jsx"},{"name":"RANGE_PRESETS","sourcePath":"components/forms/DatePicker.jsx"},{"name":"DatePicker","sourcePath":"components/forms/DatePicker.jsx"},{"name":"Field","sourcePath":"components/forms/Field.jsx"},{"name":"FileUpload","sourcePath":"components/forms/FileUpload.jsx"},{"name":"Input","sourcePath":"components/forms/Input.jsx"},{"name":"InputGroup","sourcePath":"components/forms/InputGroup.jsx"},{"name":"RichText","sourcePath":"components/forms/RichText.jsx"},{"name":"Segmented","sourcePath":"components/forms/Segmented.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Stepper","sourcePath":"components/forms/Stepper.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"Textarea","sourcePath":"components/forms/Textarea.jsx"},{"name":"Cluster","sourcePath":"components/layout/Cluster.jsx"},{"name":"Container","sourcePath":"components/layout/Container.jsx"},{"name":"Grid","sourcePath":"components/layout/Grid.jsx"},{"name":"Page","sourcePath":"components/layout/Page.jsx"},{"name":"Section","sourcePath":"components/layout/Section.jsx"},{"name":"Split","sourcePath":"components/layout/Split.jsx"},{"name":"Stack","sourcePath":"components/layout/Stack.jsx"},{"name":"FeatureGrid","sourcePath":"components/marketing/FeatureGrid.jsx"},{"name":"Hero","sourcePath":"components/marketing/Hero.jsx"},{"name":"LogoRail","sourcePath":"components/marketing/LogoRail.jsx"},{"name":"StoryCard","sourcePath":"components/marketing/StoryCard.jsx"},{"name":"AppBar","sourcePath":"components/nav/AppBar.jsx"},{"name":"BottomNav","sourcePath":"components/nav/BottomNav.jsx"},{"name":"Breadcrumbs","sourcePath":"components/nav/Breadcrumbs.jsx"},{"name":"CommandPalette","sourcePath":"components/nav/CommandPalette.jsx"},{"name":"Sidebar","sourcePath":"components/nav/Sidebar.jsx"},{"name":"NavItem","sourcePath":"components/nav/Sidebar.jsx"},{"name":"Tabs","sourcePath":"components/nav/Tabs.jsx"},{"name":"ThemeToggle","sourcePath":"components/nav/ThemeToggle.jsx"},{"name":"TopBar","sourcePath":"components/nav/TopBar.jsx"},{"name":"Dialog","sourcePath":"components/overlays/Dialog.jsx"},{"name":"Drawer","sourcePath":"components/overlays/Drawer.jsx"},{"name":"Menu","sourcePath":"components/overlays/Menu.jsx"},{"name":"Popover","sourcePath":"components/overlays/Popover.jsx"},{"name":"Toast","sourcePath":"components/overlays/Toast.jsx"},{"name":"ToastRegion","sourcePath":"components/overlays/Toast.jsx"},{"name":"Tooltip","sourcePath":"components/overlays/Tooltip.jsx"},{"name":"Avatar","sourcePath":"components/primitives/Avatar.jsx"},{"name":"Button","sourcePath":"components/primitives/Button.jsx"},{"name":"Card","sourcePath":"components/primitives/Card.jsx"},{"name":"CardHead","sourcePath":"components/primitives/Card.jsx"},{"name":"CardTitle","sourcePath":"components/primitives/Card.jsx"},{"name":"CardBody","sourcePath":"components/primitives/Card.jsx"},{"name":"CardFoot","sourcePath":"components/primitives/Card.jsx"},{"name":"Chip","sourcePath":"components/primitives/Chip.jsx"},{"name":"Eyebrow","sourcePath":"components/primitives/Eyebrow.jsx"},{"name":"Icon","sourcePath":"components/primitives/Icon.jsx"},{"name":"IconNames","sourcePath":"components/primitives/Icon.jsx"},{"name":"Skeleton","sourcePath":"components/primitives/Skeleton.jsx"}],"sourceHashes":{"brand.js":"990b0500968e","components/ai/AgentTrace.jsx":"a89cdd6dbfea","components/ai/Artifact.jsx":"f1b002442d92","components/ai/ConfidenceMeter.jsx":"30190dcfb803","components/ai/DiffReview.jsx":"4e09607a1317","components/ai/Feedback.jsx":"def875e5cf4b","components/ai/Message.jsx":"e507e32b90ca","components/ai/PromptInput.jsx":"93ca1175d24c","components/ai/SourceChip.jsx":"b8ec3e12c238","components/ai/SourceList.jsx":"9a79cef91879","components/ai/ToolCall.jsx":"59464aea783b","components/data/ActivityFeed.jsx":"7a2e981faec4","components/data/BarChart.jsx":"9f9d3e59dcce","components/data/CodeBlock.jsx":"2f9b30b87102","components/data/Console.jsx":"6528c9c3ca1e","components/data/DataGrid.jsx":"be86afd0e042","components/data/EmptyState.jsx":"7340a557f731","components/data/FilterBar.jsx":"f994b3f9efb3","components/data/KpiTile.jsx":"4568571b1cff","components/data/LineChart.jsx":"802e0e4bb7bb","components/data/Pagination.jsx":"549375d68a8e","components/data/Progress.jsx":"eb02438a3b31","components/data/StatMeter.jsx":"05de231e7204","components/data/StateView.jsx":"b4e6e90a7ded","components/data/Table.jsx":"abf9e9207f5c","components/data/chart-parts.jsx":"c161224e4566","components/forms/Calendar.jsx":"4e60fcdfe224","components/forms/Checkbox.jsx":"9dc71ab81385","components/forms/Combobox.jsx":"474f4a8f9c51","components/forms/DatePicker.jsx":"4a9f966d18b6","components/forms/Field.jsx":"68955fcbdab7","components/forms/FileUpload.jsx":"3be82c97d995","components/forms/Input.jsx":"c8f27d42ff08","components/forms/InputGroup.jsx":"71d8de495436","components/forms/RichText.jsx":"5ad519152e41","components/forms/Segmented.jsx":"810909b20ac4","components/forms/Select.jsx":"6698f9fab7fd","components/forms/Stepper.jsx":"646dd2a208b6","components/forms/Switch.jsx":"7a253c25a556","components/forms/Textarea.jsx":"4bd5398cea81","components/layout/Cluster.jsx":"2e15367fe28d","components/layout/Container.jsx":"6eda4bc56e54","components/layout/Grid.jsx":"8a093fe6ed4b","components/layout/Page.jsx":"51d1753d201a","components/layout/Section.jsx":"4507aff3c57c","components/layout/Split.jsx":"6786154d4ebd","components/layout/Stack.jsx":"005a591d8c24","components/marketing/FeatureGrid.jsx":"09e859da09fb","components/marketing/Hero.jsx":"a7b4f7392c59","components/marketing/LogoRail.jsx":"1bedd4cadb90","components/marketing/StoryCard.jsx":"9a4eccdac2e3","components/nav/AppBar.jsx":"b1f55c9de347","components/nav/BottomNav.jsx":"268c05df5378","components/nav/Breadcrumbs.jsx":"b7355573cb76","components/nav/CommandPalette.jsx":"c42661a9a877","components/nav/Sidebar.jsx":"44c5097df736","components/nav/Tabs.jsx":"2bd3962fd196","components/nav/ThemeToggle.jsx":"e83ecd5c8697","components/nav/TopBar.jsx":"6d5288e5af01","components/overlays/Dialog.jsx":"f4180aef8278","components/overlays/Drawer.jsx":"5fd4b5c57b29","components/overlays/Menu.jsx":"f98a7cea9ed8","components/overlays/Popover.jsx":"7ed814557ce6","components/overlays/Toast.jsx":"393664553347","components/overlays/Tooltip.jsx":"7b19e7dc9711","components/primitives/Avatar.jsx":"277ea4c7ca39","components/primitives/Button.jsx":"771ccb11e4a8","components/primitives/Card.jsx":"81ca43059ba9","components/primitives/Chip.jsx":"458e477fd284","components/primitives/Eyebrow.jsx":"ac1cc5e5856f","components/primitives/Icon.jsx":"715c4d4fb998","components/primitives/Skeleton.jsx":"bcdfbd3a35f3","hooks.js":"cd66047870d7","react.js":"96067cc59a1c","tailwind-preset.cjs":"58b2cfdfd063"},"inlinedExternals":[],"unexposedExports":[{"name":"animateCounter","sourcePath":"hooks.js"},{"name":"brandRamp","sourcePath":"brand.js"},{"name":"brandVars","sourcePath":"brand.js"},{"name":"clampedHex","sourcePath":"brand.js"},{"name":"contrast","sourcePath":"brand.js"},{"name":"cx","sourcePath":"components/data/chart-parts.jsx"},{"name":"formatBytes","sourcePath":"components/forms/FileUpload.jsx"},{"name":"hslToRgb","sourcePath":"brand.js"},{"name":"iconNames","sourcePath":"components/primitives/Icon.jsx"},{"name":"isInBand","sourcePath":"brand.js"},{"name":"luminance","sourcePath":"brand.js"},{"name":"nf","sourcePath":"components/data/chart-parts.jsx"},{"name":"parseHex","sourcePath":"brand.js"},{"name":"rgbToHsl","sourcePath":"brand.js"},{"name":"score","sourcePath":"components/nav/CommandPalette.jsx"},{"name":"ticks","sourcePath":"components/data/chart-parts.jsx"},{"name":"timeAgo","sourcePath":"components/data/ActivityFeed.jsx"},{"name":"useDeterministicCascade","sourcePath":"hooks.js"},{"name":"useReducedMotion","sourcePath":"hooks.js"},{"name":"useReveal","sourcePath":"hooks.js"},{"name":"useSpotlight","sourcePath":"hooks.js"},{"name":"useTheme","sourcePath":"hooks.js"}]} */

(() => {

const __ds_ns = (window.LeanWiseDesign_f2d907 = window.LeanWiseDesign_f2d907 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// brand.js
try { (() => {
/**
 * Per-tenant theming — @leanwise/design/brand
 *
 *   import { brandVars } from "@leanwise/design/brand";
 *   <div style={brandVars(org?.accent, resolvedTheme)}>…</div>
 *
 * Scope it to the WORKSPACE, never to <html>: a tenant colour is a property of
 * the workspace you are looking at, and putting it on the root means the app
 * chrome, the theme picker and any second workspace on screen all inherit it.
 *
 * Returns {} when there is no tenant colour, so tokens fall through to LeanWise
 * cyan — there is no "no-brand" state to design.
 */

/* ---- Why this returns tiers AND derived values -----------------------------
   tokens.css authors a channel triple and derives the usable colour from it:

     --lw-brand-500-c: 185 82% 26.5%;
     --lw-brand-500:   hsl(var(--lw-brand-500-c));

   A custom property is substituted at computed-value time on the element that
   DECLARES it. --lw-brand-500 is declared at :root, so it resolves against
   :root's channel and inherits as a finished colour. Overriding the channel on
   a subtree therefore does NOT move the derived value — the two must both be
   set here. Anything that reads a channel through var() at use time (alpha
   mixes like hsl(var(--primary) / .15)) follows the channel on its own.
   ---------------------------------------------------------------------------- */

/* Legibility band. Below 20% a fill is indistinguishable from the navy ground;
   above 50% white ink drops under AA and the fill starts reading as the amber
   CTA's peer. Saturation is floored because a tenant grey makes every brand
   surface look like a disabled state. */
const L_MIN = 20,
  L_MAX = 50,
  S_MIN = 26,
  S_MAX = 92;

/* Below this the input carries no hue, and hue is what a brand override is FOR.
   Flooring the saturation of #888888 would invent one (hue 0 → a red-brown
   nobody chose), so an achromatic pick falls through to LeanWise cyan instead —
   the same answer as no pick at all. */
const S_ACHROMATIC = 8;
const clamp = (n, lo, hi) => Math.min(hi, Math.max(lo, n));
const round = (n, p = 1) => Math.round(n * 10 ** p) / 10 ** p;

/** #rgb / #rrggbb / rrggbb → [r,g,b] 0-255, or null when unparseable. */
function parseHex(hex) {
  if (typeof hex !== "string") return null;
  let h = hex.trim().replace(/^#/, "");
  if (h.length === 3) h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2];
  if (!/^[0-9a-fA-F]{6}$/.test(h)) return null;
  return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)];
}

/** WCAG relative luminance. */
function luminance([r, g, b]) {
  const f = v => {
    v /= 255;
    return v <= 0.03928 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4;
  };
  return 0.2126 * f(r) + 0.7152 * f(g) + 0.0722 * f(b);
}

/** WCAG contrast ratio between two rgb triples. */
function contrast(a, b) {
  const [hi, lo] = [luminance(a), luminance(b)].sort((x, y) => y - x);
  return (hi + 0.05) / (lo + 0.05);
}
function rgbToHsl([r, g, b]) {
  r /= 255;
  g /= 255;
  b /= 255;
  const max = Math.max(r, g, b),
    min = Math.min(r, g, b),
    d = max - min,
    l = (max + min) / 2;
  let h = 0,
    s = 0;
  if (d) {
    s = d / (1 - Math.abs(2 * l - 1));
    h = max === r ? (g - b) / d + (g < b ? 6 : 0) : max === g ? (b - r) / d + 2 : (r - g) / d + 4;
    h *= 60;
  }
  return [h, s * 100, l * 100];
}
function hslToRgb([h, s, l]) {
  s /= 100;
  l /= 100;
  const k = n => (n + h / 30) % 12;
  const a = s * Math.min(l, 1 - l);
  const f = n => l - a * Math.max(-1, Math.min(k(n) - 3, Math.min(9 - k(n), 1)));
  return [f(0), f(8), f(4)].map(v => Math.round(v * 255));
}
const triple = ([h, s, l]) => `${round(h)} ${round(s)}% ${round(l)}%`;

/* The ink on a tenant fill is measured against the CLAMPED colour, not the one
   the customer typed — the clamp is what will actually be painted. White vs the
   navy ink, whichever clears by more; identical rule to the token core. */
const WHITE = [255, 255, 255],
  NAVY = [11, 18, 32];

/** The 51-component brand ramp, synthesized from one clamped anchor. */
function brandRamp(hex) {
  const rgb = parseHex(hex);
  if (!rgb) return null;
  const [h, s0, l0] = rgbToHsl(rgb);
  if (s0 < S_ACHROMATIC) return null;
  const s = clamp(s0, S_MIN, S_MAX);
  const l = clamp(l0, L_MIN, L_MAX);
  const anchor = [h, s, l];
  const tier = (dl, ds = 0) => [h, clamp(s + ds, 0, 100), clamp(l + dl, 4, 97)];
  return {
    anchor,
    ink: contrast(hslToRgb(anchor), WHITE) >= contrast(hslToRgb(anchor), NAVY) ? WHITE : NAVY,
    tiers: {
      50: tier(70 - l * 0.28, -14),
      100: tier(64 - l * 0.28, -16),
      200: tier(54 - l * 0.26, -18),
      300: tier(38 - l * 0.2, -22),
      400: tier(21, -20),
      500: anchor,
      600: tier(-5.5, 4),
      700: tier(-10, 6)
    }
  };
}

/**
 * The style object. `scheme` is the RESOLVED theme ("light" | "dark") and only
 * decides which tier answers as brand TEXT — the fill tiers are theme-agnostic.
 * Pass useTheme().resolved so it re-renders when the theme changes; the default
 * is "light" because that is what a server render should commit to.
 */
function brandVars(hex, scheme = "light") {
  const ramp = brandRamp(hex);
  if (!ramp) return {};
  const {
    tiers,
    ink
  } = ramp;
  const out = {};
  for (const [k, v] of Object.entries(tiers)) {
    out[`--lw-brand-${k}-c`] = triple(v);
    out[`--lw-brand-${k}`] = `hsl(${triple(v)})`;
  }
  const text = scheme === "dark" ? tiers[400] : tiers[500];
  out["--lw-brand-text-c"] = triple(text);
  out["--lw-brand-text"] = `hsl(${triple(text)})`;
  /* Rule 3 of the README: a tenant theme owns --primary and --ring. It never
     touches --accent, which is shadcn's hover SURFACE, not a brand colour. */
  out["--primary"] = triple(tiers[500]);
  out["--primary-foreground"] = ink === WHITE ? "0 0% 100%" : "220.5 48.9% 8.4%";
  out["--ring"] = triple(scheme === "dark" ? tiers[400] : tiers[500]);
  /* Solid, matching the shipped token — a tenant's ring must not be the one washed
     focus state in the system. Tier follows the scheme, as --ring already does. */
  out["--lw-focus-ring"] = `0 0 0 2px hsl(${triple(tiers[scheme === "dark" ? 400 : 500])})`;
  return out;
}

/** The hex actually painted after clamping — for a "this is your colour" swatch. */
function clampedHex(hex) {
  const ramp = brandRamp(hex);
  if (!ramp) return null;
  return "#" + hslToRgb(ramp.anchor).map(v => v.toString(16).padStart(2, "0")).join("");
}

/** True when the tenant's own hex survived the clamp untouched. */
function isInBand(hex) {
  const rgb = parseHex(hex);
  if (!rgb) return false;
  const [, s, l] = rgbToHsl(rgb);
  return s >= S_MIN && s <= S_MAX && l >= L_MIN && l <= L_MAX;
}
Object.assign(__ds_scope, { parseHex, luminance, contrast, rgbToHsl, hslToRgb, brandRamp, brandVars, clampedHex, isInBand });
})(); } catch (e) { __ds_ns.__errors.push({ path: "brand.js", error: String((e && e.message) || e) }); }

// components/ai/AgentTrace.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The agent's steps on a hairline spine. Users trust what they can watch —
 *  and a trace is the cheapest way to make a slow answer feel accountable. */
function AgentTrace({
  steps = [],
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", _extends({
    className: cx("lw-trace", className)
  }, rest), steps.map((s, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    "data-state": s.state || "pending"
  }, /*#__PURE__*/React.createElement("span", {
    className: "step"
  }, s.label), s.meta && /*#__PURE__*/React.createElement("span", {
    className: "meta"
  }, s.meta))));
}
Object.assign(__ds_scope, { AgentTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/AgentTrace.jsx", error: String((e && e.message) || e) }); }

// components/ai/ConfidenceMeter.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** A retrieval score. Mono number plus a bar: a bar alone is unquotable, a
 *  number alone is unscannable. Below 60% it drops to neutral ink — a low
 *  score painted brand-cyan reads as endorsement. */
function ConfidenceMeter({
  value = 0,
  label = "match",
  className,
  style,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, Math.round(value)));
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-confidence", pct < 60 && "low", className),
    style: {
      "--lw-confidence": pct + "%",
      ...style
    },
    role: "meter",
    "aria-valuenow": pct,
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    "aria-label": label
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "rail",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("i", null)), /*#__PURE__*/React.createElement("span", null, pct, "%"));
}
Object.assign(__ds_scope, { ConfidenceMeter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/ConfidenceMeter.jsx", error: String((e && e.message) || e) }); }

// components/ai/PromptInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The AI product's primary input. A framed box, not a bare textarea, so the
 * attachment row and the send action live inside the same focus ring.
 *
 * Enter submits, Shift+Enter newlines — the convention users already have.
 *
 * The footer has two modes. Passing `tools`/`action` keeps the standard
 * arrangement (tools left, hint and action right). Passing children replaces
 * the row outright, for the products that need the model picker, scope
 * selector and send button in a specific order — one row, one owner, rather
 * than a slot per control.
 */
function PromptInput({
  value,
  onChange,
  onSubmit,
  placeholder = "Ask anything about your documents…",
  hint = "⏎ to send · ⇧⏎ newline",
  label = "Prompt",
  tools,
  action,
  disabled,
  className,
  children,
  ...rest
}) {
  const onKeyDown = e => {
    // The disabled guard matters more here than on a button: a disabled
    // textarea still receives keydown in some engines, and the send path is a
    // network call, so an unguarded Enter is a duplicate request.
    if (disabled) return;
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      onSubmit && onSubmit();
    }
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-prompt", className)
  }, rest), /*#__PURE__*/React.createElement("textarea", {
    value: value,
    onChange: e => onChange && onChange(e.target.value),
    onKeyDown: onKeyDown,
    placeholder: placeholder,
    rows: 2,
    disabled: disabled,
    "aria-label": label
  }), /*#__PURE__*/React.createElement("div", {
    className: "lw-prompt-foot"
  }, children || /*#__PURE__*/React.createElement(React.Fragment, null, tools, /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-prompt-hint"
  }, hint), action)));
}
Object.assign(__ds_scope, { PromptInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/PromptInput.jsx", error: String((e && e.message) || e) }); }

// components/ai/SourceChip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The citation atom. A numbered mono chip so a claim in the answer and the
 *  document behind it share one identifier the user can point at. */
function SourceChip({
  n,
  title,
  as,
  className,
  ...rest
}) {
  const Tag = as || (rest.href ? "a" : "button");
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-source", className),
    type: Tag === "button" ? "button" : undefined,
    "aria-label": title ? `Source ${n}: ${title}` : `Source ${n}`
  }, rest), n);
}
Object.assign(__ds_scope, { SourceChip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/SourceChip.jsx", error: String((e && e.message) || e) }); }

// components/ai/SourceList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The panel beside the answer. Each entry carries where it came from — an
 *  answer whose provenance is not inspectable is not verifiable. */
function SourceList({
  sources = [],
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-source-list", className)
  }, rest), sources.map((s, i) => {
    // An <a> with no href is not focusable and not announced as a control —
    // so an item without a URL renders as the button it actually is.
    const Tag = s.href ? "a" : "button";
    return /*#__PURE__*/React.createElement(Tag, {
      key: s.id ?? i,
      className: "lw-source-item",
      href: s.href || undefined,
      type: s.href ? undefined : "button",
      onClick: s.onClick
    }, /*#__PURE__*/React.createElement("span", {
      className: "n"
    }, s.n ?? i + 1), /*#__PURE__*/React.createElement("span", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      className: "t"
    }, s.title), s.meta && /*#__PURE__*/React.createElement("span", {
      className: "m"
    }, s.meta)));
  }));
}
Object.assign(__ds_scope, { SourceList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/SourceList.jsx", error: String((e && e.message) || e) }); }

// components/data/Console.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The browser-frame motif: chrome, a mono stream, an optional status foot.
 *
 *  A log line is either `text` (one free-running string) or `cells` — an array
 *  of fields that align into real grid columns across every line. Use `cells`
 *  for anything tabular: padding a mono string with runs of spaces looks aligned
 *  in the source and is wrong the moment one value is longer than the author
 *  guessed, or the frame gets narrow enough to re-wrap. A cell may be a plain
 *  string, or { text, num, muted } — `num` right-aligns and tabularises.
 *
 *  The two shapes mix freely: a plain `text` line inside a columned log spans
 *  every field column rather than being squeezed into the first one, so a
 *  free-running status line can sit between two tabular ones. */
function Console({
  url = "leanwise.ai",
  title,
  lines,
  foot,
  className,
  children,
  ...rest
}) {
  // Columns: the timestamp gutter, then one per cell, then a flexible spacer.
  // Every content column hugs, so a right-aligned number lands just past the
  // longest detail rather than at the far edge of the frame — a figure flung to
  // the opposite side of a wide console reads as belonging to no row at all.
  // The last cell column may shrink below max-content so long free text wraps
  // instead of overflowing the frame.
  const cellCount = lines ? lines.reduce((n, l) => Math.max(n, l.cells ? l.cells.length : 0), 0) : 0;
  const logStyle = cellCount ? {
    gridTemplateColumns: "max-content " + "max-content ".repeat(Math.max(0, cellCount - 1)) + "minmax(0, max-content) 1fr"
  } : undefined;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-console", className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-console-h"
  }, /*#__PURE__*/React.createElement("span", {
    className: "left"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lights"
  }, /*#__PURE__*/React.createElement("i", null), /*#__PURE__*/React.createElement("i", null), /*#__PURE__*/React.createElement("i", null)), title), url && /*#__PURE__*/React.createElement("span", {
    className: "url"
  }, url)), /*#__PURE__*/React.createElement("div", {
    className: "lw-console-body"
  }, lines ? /*#__PURE__*/React.createElement("div", {
    className: "lw-console-log",
    role: "log",
    style: logStyle
  }, lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: cx("lw-console-line", l.tone)
  }, (l.t || cellCount > 0) && /*#__PURE__*/React.createElement("span", {
    className: "t"
  }, l.t), l.cells ? l.cells.map((c, j) => {
    const cell = typeof c === "string" ? {
      text: c
    } : c || {};
    return /*#__PURE__*/React.createElement("span", {
      key: j,
      className: cx("lw-console-cell", cell.num && "num", cell.muted && "muted")
    }, cell.text);
  }) : /*#__PURE__*/React.createElement("span", {
    className: "lw-console-span"
  }, l.text)))) : children), foot && /*#__PURE__*/React.createElement("div", {
    className: "lw-console-foot"
  }, foot));
}
Object.assign(__ds_scope, { Console });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Console.jsx", error: String((e && e.message) || e) }); }

// components/data/Progress.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Determinate progress. A component rather than a styled span, for one reason:
 * the fill width is a live value, and a live value belongs in a component that
 * owns it — not in a template's style attribute, where nothing can paint until
 * the first number arrives.
 *
 * `role="progressbar"` with real values, so a screen reader gets the number and
 * not just a moving box. Indeterminate work is a `Skeleton`, not this: a bar
 * that animates without knowing how far along it is tells the user a number it
 * does not have.
 */
function Progress({
  value = 0,
  max = 100,
  label,
  tone,
  className,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, Number(value) / Number(max || 100) * 100));
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-progress", className),
    "data-tone": tone,
    role: "progressbar",
    "aria-valuenow": Math.round(pct),
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    "aria-label": label
  }, rest), /*#__PURE__*/React.createElement("i", {
    style: {
      width: pct + "%"
    }
  }));
}
Object.assign(__ds_scope, { Progress });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Progress.jsx", error: String((e && e.message) || e) }); }

// components/data/chart-parts.jsx
try { (() => {
const cx = (...a) => a.filter(Boolean).join(" ");
const SERIES = i => "var(--lw-chart-" + (i % 8 + 1) + ")";
const nf = new Intl.NumberFormat();

/* Every chart ships the numbers behind it. A picture of data is not readable by
   a screen reader, and a one-line summary is not the data — so the table is the
   real content and the SVG is the presentation of it. */
function DataTable({
  labels,
  series,
  caption
}) {
  return /*#__PURE__*/React.createElement("table", {
    className: "lw-sr-only"
  }, /*#__PURE__*/React.createElement("caption", null, caption), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    scope: "col"
  }, "Category"), series.map(s => /*#__PURE__*/React.createElement("th", {
    key: s.name,
    scope: "col"
  }, s.name)))), /*#__PURE__*/React.createElement("tbody", null, labels.map((l, i) => /*#__PURE__*/React.createElement("tr", {
    key: l
  }, /*#__PURE__*/React.createElement("th", {
    scope: "row"
  }, l), series.map(s => /*#__PURE__*/React.createElement("td", {
    key: s.name
  }, nf.format(s.data[i])))))));
}
function Legend({
  series
}) {
  if (series.length < 2) return null;
  return /*#__PURE__*/React.createElement("div", {
    className: "lw-chart-legend"
  }, series.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: s.name
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      background: s.color || SERIES(i)
    }
  }), s.name)));
}
const ticks = (max, n = 4) => {
  const step = Math.pow(10, Math.floor(Math.log10(max / n || 1)));
  const s = Math.ceil(max / n / step) * step;
  return Array.from({
    length: n + 1
  }, (_, i) => i * s);
};
Object.assign(__ds_scope, { cx, SERIES, nf, DataTable, Legend, ticks });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/chart-parts.jsx", error: String((e && e.message) || e) }); }

// components/data/BarChart.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * A thin tokenised chart layer, not a charting engine — for the two shapes a
 * product dashboard actually needs. Anything richer takes a real library; this
 * exists so a KPI strip does not each time.
 */
function BarChart({
  labels = [],
  series = [],
  height = 200,
  stacked,
  label,
  className,
  ...rest
}) {
  const w = 640,
    pad = {
      t: 8,
      r: 8,
      b: 22,
      l: 40
    };
  const max = Math.max(1, ...series.flatMap(s => stacked ? [] : s.data), ...(stacked ? labels.map((_, i) => series.reduce((a, s) => a + s.data[i], 0)) : []));
  const ts = __ds_scope.ticks(max);
  const top = ts[ts.length - 1];
  const iw = w - pad.l - pad.r,
    ih = height - pad.t - pad.b;
  const bandW = iw / Math.max(labels.length, 1);
  const barW = stacked ? bandW * 0.56 : bandW * 0.72 / Math.max(series.length, 1);
  const y = v => pad.t + ih - v / top * ih;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: __ds_scope.cx("lw-chart-wrap", className)
  }, rest), /*#__PURE__*/React.createElement("svg", {
    className: "lw-chart",
    viewBox: "0 0 " + w + " " + height,
    role: "img",
    "aria-label": label
  }, /*#__PURE__*/React.createElement("g", {
    className: "grid"
  }, ts.map(v => /*#__PURE__*/React.createElement("line", {
    key: v,
    x1: pad.l,
    x2: w - pad.r,
    y1: y(v),
    y2: y(v)
  }))), /*#__PURE__*/React.createElement("g", {
    className: "axis"
  }, ts.map(v => /*#__PURE__*/React.createElement("text", {
    key: v,
    x: pad.l - 6,
    y: y(v) + 3,
    textAnchor: "end"
  }, __ds_scope.nf.format(v))), labels.map((l, i) => /*#__PURE__*/React.createElement("text", {
    key: l,
    x: pad.l + bandW * i + bandW / 2,
    y: height - 6,
    textAnchor: "middle"
  }, l))), labels.map((l, i) => {
    let acc = 0;
    return series.map((s, si) => {
      const v = s.data[i] || 0;
      const h = v / top * ih;
      const x = stacked ? pad.l + bandW * i + (bandW - barW) / 2 : pad.l + bandW * i + (bandW - barW * series.length) / 2 + barW * si;
      const yy = stacked ? pad.t + ih - acc - h : y(v);
      acc += h;
      return /*#__PURE__*/React.createElement("rect", {
        key: s.name + i,
        className: "bar",
        x: x,
        y: yy,
        width: barW,
        height: Math.max(0, h),
        rx: "2",
        fill: s.color || __ds_scope.SERIES(si)
      }, /*#__PURE__*/React.createElement("title", null, s.name + " · " + l + " · " + __ds_scope.nf.format(v)));
    });
  })), /*#__PURE__*/React.createElement(__ds_scope.Legend, {
    series: series
  }), /*#__PURE__*/React.createElement(__ds_scope.DataTable, {
    labels: labels,
    series: series,
    caption: label
  }));
}
Object.assign(__ds_scope, { BarChart });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/BarChart.jsx", error: String((e && e.message) || e) }); }

// components/data/LineChart.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** The line and area chart. Same tokenised layer as BarChart. */
function LineChart({
  labels = [],
  series = [],
  height = 200,
  area,
  label,
  className,
  ...rest
}) {
  const w = 640,
    pad = {
      t: 8,
      r: 8,
      b: 22,
      l: 40
    };
  const max = Math.max(1, ...series.flatMap(s => s.data));
  const ts = __ds_scope.ticks(max);
  const top = ts[ts.length - 1];
  const iw = w - pad.l - pad.r,
    ih = height - pad.t - pad.b;
  const x = i => pad.l + (labels.length < 2 ? iw / 2 : iw / (labels.length - 1) * i);
  const y = v => pad.t + ih - v / top * ih;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: __ds_scope.cx("lw-chart-wrap", className)
  }, rest), /*#__PURE__*/React.createElement("svg", {
    className: "lw-chart",
    viewBox: "0 0 " + w + " " + height,
    role: "img",
    "aria-label": label
  }, /*#__PURE__*/React.createElement("g", {
    className: "grid"
  }, ts.map(v => /*#__PURE__*/React.createElement("line", {
    key: v,
    x1: pad.l,
    x2: w - pad.r,
    y1: y(v),
    y2: y(v)
  }))), /*#__PURE__*/React.createElement("g", {
    className: "axis"
  }, ts.map(v => /*#__PURE__*/React.createElement("text", {
    key: v,
    x: pad.l - 6,
    y: y(v) + 3,
    textAnchor: "end"
  }, __ds_scope.nf.format(v))), labels.map((l, i) => /*#__PURE__*/React.createElement("text", {
    key: l,
    x: x(i),
    y: height - 6,
    textAnchor: "middle"
  }, l))), series.map((s, si) => {
    const d = s.data.map((v, i) => (i ? "L" : "M") + x(i) + " " + y(v)).join(" ");
    const c = s.color || __ds_scope.SERIES(si);
    return /*#__PURE__*/React.createElement("g", {
      key: s.name
    }, area && /*#__PURE__*/React.createElement("path", {
      d: d + " L" + x(s.data.length - 1) + " " + (pad.t + ih) + " L" + x(0) + " " + (pad.t + ih) + " Z",
      fill: c,
      opacity: "0.12"
    }), /*#__PURE__*/React.createElement("path", {
      className: "line",
      d: d,
      stroke: c
    }), s.data.map((v, i) => /*#__PURE__*/React.createElement("circle", {
      key: i,
      className: "dot",
      cx: x(i),
      cy: y(v),
      r: "3",
      fill: c
    }, /*#__PURE__*/React.createElement("title", null, s.name + " · " + labels[i] + " · " + __ds_scope.nf.format(v)))));
  })), /*#__PURE__*/React.createElement(__ds_scope.Legend, {
    series: series
  }), /*#__PURE__*/React.createElement(__ds_scope.DataTable, {
    labels: labels,
    series: series,
    caption: label
  }));
}
Object.assign(__ds_scope, { LineChart });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/LineChart.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
function Checkbox({
  label,
  radio = false,
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("label", {
    className: cx("lw-check", radio && "radio", className)
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: radio ? "radio" : "checkbox"
  }, rest)), /*#__PURE__*/React.createElement("span", {
    className: "box"
  }), label && /*#__PURE__*/React.createElement("span", {
    className: "lw-check-text"
  }, label));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/Field.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The form unit: label, control, and ONE slot that holds help text or an error
 * — never both, because a field showing both is a field whose error is easy to
 * miss. Wires htmlFor/id and aria-describedby so the control does not have to.
 */
function Field({
  label,
  help,
  error,
  required,
  optional,
  htmlFor,
  className,
  children,
  ...rest
}) {
  const id = htmlFor;
  const msgId = id ? id + "-msg" : undefined;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-field", className)
  }, rest), label && /*#__PURE__*/React.createElement("label", {
    className: "lw-label",
    htmlFor: id
  }, label, required && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "req",
    "aria-hidden": "true"
  }, "*"), /*#__PURE__*/React.createElement("span", {
    className: "lw-sr-only"
  }, "(required)")), optional && /*#__PURE__*/React.createElement("span", {
    className: "opt"
  }, "optional")), typeof children === "function" ? children({
    id,
    "aria-describedby": msgId,
    "aria-invalid": error ? "true" : undefined,
    required
  }) : children, error ? /*#__PURE__*/React.createElement("span", {
    className: "lw-error",
    id: msgId,
    role: "alert"
  }, error) : help ? /*#__PURE__*/React.createElement("span", {
    className: "lw-help",
    id: msgId
  }, help) : null);
}
Object.assign(__ds_scope, { Field });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Field.jsx", error: String((e && e.message) || e) }); }

// components/forms/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
function Input({
  size = "md",
  invalid,
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("input", _extends({
    className: cx("lw-input", size === "sm" && "lw-input-sm", size === "lg" && "lw-input-lg", className),
    "aria-invalid": invalid ? "true" : undefined
  }, rest));
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Input.jsx", error: String((e && e.message) || e) }); }

// components/forms/InputGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** An input with an affix. The BOX owns the border and the focus ring, so the
 *  ring wraps the whole control rather than a rectangle inside it — and the
 *  invalid state is selected with `:has([aria-invalid])`, so the attribute the
 *  screen reader reads is the same one that paints the border. */
function InputGroup({
  prefix,
  suffix,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-input-group", className)
  }, rest), prefix && /*#__PURE__*/React.createElement("span", {
    className: "affix"
  }, prefix), children, suffix && /*#__PURE__*/React.createElement("span", {
    className: "affix mono"
  }, suffix));
}
Object.assign(__ds_scope, { InputGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/InputGroup.jsx", error: String((e && e.message) || e) }); }

// components/forms/Segmented.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** 2-4 mutually exclusive views. Above four, use Select — a segmented control
 *  that scrolls has stopped being a segmented control. */
function Segmented({
  options = [],
  value,
  onChange,
  label,
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-segmented", className),
    role: "group",
    "aria-label": label
  }, rest), options.map(o => {
    const v = typeof o === "string" ? o : o.value;
    const l = typeof o === "string" ? o : o.label;
    return /*#__PURE__*/React.createElement("button", {
      key: v,
      type: "button",
      "aria-pressed": value === v,
      onClick: () => onChange && onChange(v)
    }, l);
  }));
}
Object.assign(__ds_scope, { Segmented });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Segmented.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The chevron is drawn in CSS: the native arrow is the one control detail that
 *  gives the platform away and cannot be tokenised. */
function Select({
  options,
  invalid,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("select", _extends({
    className: cx("lw-select", className),
    "aria-invalid": invalid ? "true" : undefined
  }, rest), options ? options.map(o => {
    const v = typeof o === "string" ? o : o.value;
    const l = typeof o === "string" ? o : o.label;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  }) : children);
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** For a setting that applies immediately. A checkbox is for a value you
 *  submit; the two are not interchangeable and the shapes say which is which. */
function Switch({
  label,
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("label", {
    className: cx("lw-switch", className)
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    role: "switch"
  }, rest)), /*#__PURE__*/React.createElement("span", {
    className: "track"
  }), label && /*#__PURE__*/React.createElement("span", {
    className: "lw-switch-text"
  }, label));
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/forms/Textarea.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
function Textarea({
  invalid,
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("textarea", _extends({
    className: cx("lw-textarea", className),
    "aria-invalid": invalid ? "true" : undefined
  }, rest));
}
Object.assign(__ds_scope, { Textarea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Textarea.jsx", error: String((e && e.message) || e) }); }

// components/layout/Cluster.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Horizontal group that wraps. Toolbars, chip rows, action bars. Wrapping is
 *  the default because a row that cannot wrap will overflow a phone. */
function Cluster({
  gap = 8,
  justify,
  align,
  as: Tag = "div",
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-cluster", gap !== 8 && `lw-cluster-${gap}`, justify === "between" && "lw-cluster-between", justify === "end" && "lw-cluster-end", align === "baseline" && "lw-cluster-baseline", className)
  }, rest), children);
}
Object.assign(__ds_scope, { Cluster });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Cluster.jsx", error: String((e && e.message) || e) }); }

// components/layout/Container.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The 1200px reading column — narrower than `Page` because prose and a shell
 *  do not want the same measure. */
function Container({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-container", className)
  }, rest), children);
}
Object.assign(__ds_scope, { Container });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Container.jsx", error: String((e && e.message) || e) }); }

// components/layout/Grid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Auto-fit columns. `min` is the only knob — the grid reflows on its own at
 *  every width, so the common case needs no breakpoint. */
function Grid({
  min,
  gap = 16,
  as: Tag = "div",
  className,
  style,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-grid", gap === 24 && "lw-grid-24", className),
    style: min ? {
      "--lw-grid-min": typeof min === "number" ? min + "px" : min,
      ...style
    } : style
  }, rest), children);
}
Object.assign(__ds_scope, { Grid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Grid.jsx", error: String((e && e.message) || e) }); }

// components/layout/Page.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The app-shell width (1400px) — wider than the reading column on purpose. */
function Page({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-page", className)
  }, rest), children);
}
Object.assign(__ds_scope, { Page });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Page.jsx", error: String((e && e.message) || e) }); }

// components/layout/Section.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** A page band. `dark` flips every descendant's ink via the band convention —
 *  never restyle children by hand for a dark ground. */
function Section({
  dark = false,
  tight = false,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    className: cx("lw-section", tight && "tight", dark && "dark lw-band-dark", className),
    "data-band": dark ? "dark" : undefined
  }, rest), children);
}
Object.assign(__ds_scope, { Section });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Section.jsx", error: String((e && e.message) || e) }); }

// components/layout/Split.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** A main column and a rail, in source order. Collapses to one column at
 *  `--lw-bp-lg` (1024px) with no query of your own. */
function Split({
  rail = 320,
  side = "end",
  as: Tag = "div",
  className,
  style,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-split", side === "start" && "lw-split-start", className),
    style: {
      "--lw-split-rail": typeof rail === "number" ? rail + "px" : rail,
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Split });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Split.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stack.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Vertical rhythm. The gap is the only thing it does. */
function Stack({
  gap = 16,
  as: Tag = "div",
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-stack", gap !== 16 && `lw-stack-${gap}`, className)
  }, rest), children);
}
Object.assign(__ds_scope, { Stack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stack.jsx", error: String((e && e.message) || e) }); }

// components/marketing/Hero.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The dark hero: deep-navy ground, masked hairline grid, radial brand wash.
 *  Descendants get on-dark ink automatically — never restyle them by hand. */
function Hero({
  eyebrow,
  title,
  lead,
  actions,
  aside,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    className: cx("lw-hero-dark", className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-container"
  }, eyebrow && /*#__PURE__*/React.createElement("p", {
    className: "lw-eyebrow"
  }, eyebrow), title && /*#__PURE__*/React.createElement("h1", {
    className: "lw-h1"
  }, title), lead && /*#__PURE__*/React.createElement("p", {
    className: "lw-lead"
  }, lead), actions && /*#__PURE__*/React.createElement("div", {
    className: "lw-cluster lw-cluster-12",
    style: {
      marginTop: "var(--lw-space-24)"
    }
  }, actions), aside, children));
}
Object.assign(__ds_scope, { Hero });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/marketing/Hero.jsx", error: String((e && e.message) || e) }); }

// components/marketing/LogoRail.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Logo wall. `.lw-logo-rail` is itself the flex row, so the cells are direct
 * children in the static case — the `.lw-logo-track` wrapper is only styled
 * under `.marquee`, and wrapping unconditionally stacks the rail vertically.
 *
 * A mark with `src` is MASKED to one ink (`--lw-logo-src`), never drawn as an
 * <img>: customer marks arrive in a dozen inks and two will clash. A mark with
 * only a name degrades to a mono wordmark — not a bordered placeholder box.
 *
 * `marquee` duplicates the track for a slow loop. The duplicate lives in the
 * markup rather than being cloned in JS, so reduced motion can simply hide it
 * and fall back to a static wrap.
 */
function LogoRail({
  logos = [],
  marquee = false,
  className,
  ...rest
}) {
  const cells = logos.map((l, i) => l.src ? /*#__PURE__*/React.createElement("span", {
    key: i,
    className: "lw-logo-item",
    style: {
      "--lw-logo-src": `url("${l.src}")`
    },
    role: "img",
    "aria-label": l.name
  }) : /*#__PURE__*/React.createElement("span", {
    key: i,
    className: "lw-logo-item is-text"
  }, l.name));
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-logo-rail", marquee && "marquee", className)
  }, rest), marquee ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "lw-logo-track"
  }, cells), /*#__PURE__*/React.createElement("div", {
    className: "lw-logo-track",
    "aria-hidden": "true"
  }, cells)) : cells);
}
Object.assign(__ds_scope, { LogoRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/marketing/LogoRail.jsx", error: String((e && e.message) || e) }); }

// components/marketing/StoryCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * A customer outcome. `.lw-story` is a two-column grid — a 64px mark tile and a
 * body column — so the body must be ONE child, not four siblings, or the fields
 * alternate across the columns.
 *
 * The quote renders ONLY when quote, person and role are all present — the
 * no-fabrication rule, enforced at runtime rather than in a doc nobody reads.
 * A half-filled testimonial is an invented one.
 */
function StoryCard({
  logo,
  title,
  body,
  result,
  quote,
  person,
  role,
  href,
  className,
  ...rest
}) {
  const Tag = href ? "a" : "div";
  const showQuote = Boolean(quote && person && role);
  const initials = String(title || "").trim().split(/\s+/).slice(0, 2).map(w => w[0] || "").join("").toUpperCase();
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-story", href && "lw-story-interactive", className),
    href: href
  }, rest), logo ? /*#__PURE__*/React.createElement("span", {
    className: "logo"
  }, logo) : /*#__PURE__*/React.createElement("span", {
    className: "logo lw-monogram"
  }, initials), /*#__PURE__*/React.createElement("div", null, title && /*#__PURE__*/React.createElement("h3", null, title), body && /*#__PURE__*/React.createElement("p", null, body), showQuote && /*#__PURE__*/React.createElement("blockquote", {
    className: "lw-story-quote"
  }, quote, /*#__PURE__*/React.createElement("cite", null, person, " \xB7 ", role)), result && /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lw-story-result"
  }, /*#__PURE__*/React.createElement("b", null, result)))));
}
Object.assign(__ds_scope, { StoryCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/marketing/StoryCard.jsx", error: String((e && e.message) || e) }); }

// components/nav/Breadcrumbs.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Mono, so it reads as a path rather than as prose.
 *
 * A Fragment, not a wrapper span: the wrapper carried `display:contents`, which
 * makes it the element `.lw-crumbs > *` matches while contributing no box — so
 * the nowrap/ellipsis rule landed on nothing and the last crumb was clipped
 * mid-word instead of truncating. The separators and labels must be the flex
 * items themselves.
 */
function Breadcrumbs({
  items = [],
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cx("lw-crumbs", className),
    "aria-label": "Breadcrumb"
  }, rest), items.map((it, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, i > 0 && /*#__PURE__*/React.createElement("span", {
    className: "sep",
    "aria-hidden": "true"
  }, "/"), it.href && i < items.length - 1 ? /*#__PURE__*/React.createElement("a", {
    href: it.href
  }, it.label) : /*#__PURE__*/React.createElement("span", {
    "aria-current": "page"
  }, it.label))));
}
Object.assign(__ds_scope, { Breadcrumbs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/Breadcrumbs.jsx", error: String((e && e.message) || e) }); }

// components/nav/Sidebar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The product rail. `collapsed` is a state on the element, not a second
 *  class, so there is one source for the width and the label visibility.
 *
 *  Keys come from `id` or the index, never from `href`. A rail's items routinely
 *  share a placeholder href (`"#"` in every template here), and React treats
 *  duplicate keys as unsupported — children may be dropped or duplicated. An
 *  href is a destination, not an identity. */
function Sidebar({
  items = [],
  collapsed = false,
  footer,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cx("lw-sidebar", className),
    "data-collapsed": collapsed ? "true" : undefined,
    "aria-label": "Sections"
  }, rest), items.map((it, i) => it.group ? /*#__PURE__*/React.createElement("span", {
    key: "g" + i,
    className: "lw-nav-group"
  }, it.group) : /*#__PURE__*/React.createElement(NavItem, _extends({
    key: it.id ?? i
  }, it, {
    collapsed: collapsed
  }))), children, footer && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "auto"
    }
  }, footer));
}
function NavItem({
  href,
  label,
  icon,
  badge,
  current,
  collapsed,
  className,
  ...rest
}) {
  const Tag = href ? "a" : "button";
  // Only a string label can become a title. String(node) yields
  // "[object Object]", which is what a collapsed rail would then show on hover.
  const tip = collapsed && typeof label === "string" ? label : undefined;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-nav-item", className),
    href: href,
    type: href ? undefined : "button",
    "aria-current": current ? "page" : undefined,
    title: tip
  }, rest), icon && /*#__PURE__*/React.createElement("span", {
    className: "ic",
    "aria-hidden": "true"
  }, icon), /*#__PURE__*/React.createElement("span", {
    className: "lw-nav-text"
  }, label), badge && /*#__PURE__*/React.createElement("span", {
    className: "badge"
  }, badge));
}
Object.assign(__ds_scope, { Sidebar, NavItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/Sidebar.jsx", error: String((e && e.message) || e) }); }

// components/nav/Tabs.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Roving-tabindex tabs with arrow-key navigation — the part every hand-rolled
 *  tab strip skips and the part a keyboard user needs.
 *
 *  Selection AND focus move together: a roving tabindex that changes the
 *  selected tab but leaves focus on the old one strands the keyboard user on a
 *  `tabindex="-1"` button, so the next arrow key does nothing. */
function Tabs({
  tabs = [],
  value,
  onChange,
  label,
  className,
  ...rest
}) {
  const ref = React.useRef(null);
  const move = next => {
    onChange && onChange(tabs[next].value);
    const el = ref.current && ref.current.querySelectorAll('[role="tab"]')[next];
    if (el) el.focus({
      preventScroll: true
    });
  };
  const onKeyDown = e => {
    const i = tabs.findIndex(t => t.value === value);
    if (i < 0) return;
    const k = e.key;
    if (k === "Home") {
      e.preventDefault();
      return move(0);
    }
    if (k === "End") {
      e.preventDefault();
      return move(tabs.length - 1);
    }
    const d = k === "ArrowRight" ? 1 : k === "ArrowLeft" ? -1 : 0;
    if (!d) return;
    e.preventDefault();
    move((i + d + tabs.length) % tabs.length);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    ref: ref,
    className: cx("lw-tabs", className),
    role: "tablist",
    "aria-label": label,
    onKeyDown: onKeyDown
  }, rest), tabs.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.value,
    role: "tab",
    type: "button",
    "aria-selected": t.value === value,
    "aria-controls": t.controls,
    id: t.id,
    tabIndex: t.value === value ? 0 : -1,
    onClick: () => onChange && onChange(t.value)
  }, t.label, t.count != null && /*#__PURE__*/React.createElement("span", {
    className: "count"
  }, t.count))));
}
Object.assign(__ds_scope, { Tabs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/nav/TopBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** App chrome. Sticky, hairline, never a shadow: a shadow under a 1px bar reads
 *  as a rendering bug on a light ground.
 *
 *  `logo` sets the MARK beside `brand` as live text, rather than dropping in the
 *  combined lockup artwork. The lockup is 2775:1000, so at bar height its
 *  wordmark caps land near 6px — illegible at any bar size worth having. The
 *  mark is a background image (per-ground artwork; its navy end vanishes on a
 *  dark bar) and the name stays real text, so it sets in the DS font at the DS
 *  size and remains editable.
 *
 *  `brandHref` makes the lockup a real <a> — the mark and the name are one link,
 *  not two, so there is a single tab stop and no dead gap between them.
 */
function TopBar({
  brand,
  brandHref,
  logo = false,
  links = [],
  actions,
  className,
  children,
  ...rest
}) {
  const Brand = brandHref ? "a" : "span";
  const brandProps = brandHref ? {
    href: brandHref,
    "aria-label": typeof brand === "string" ? brand + " — home" : "Home"
  } : {};
  return /*#__PURE__*/React.createElement("header", _extends({
    className: cx("lw-topbar", className)
  }, rest), logo ? /*#__PURE__*/React.createElement(Brand, _extends({
    className: "brand"
  }, brandProps), /*#__PURE__*/React.createElement("span", {
    className: "brand-mark",
    "aria-hidden": "true"
  }), brand) : brand && /*#__PURE__*/React.createElement(Brand, _extends({
    className: "brand"
  }, brandProps), brand), links.length > 0 && /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Primary"
  }, links.map((l, i) => /*#__PURE__*/React.createElement("a", {
    key: l.id ?? i,
    href: l.href,
    "aria-current": l.current ? "page" : undefined
  }, l.label))), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), actions, children);
}
Object.assign(__ds_scope, { TopBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/TopBar.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Popover.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* Where the panel lands, given the anchor and the space actually available.
   Pure function of two rects so it can be reasoned about without a DOM. */
function place(anchorRect, panelRect, placement, offset) {
  const vw = window.innerWidth,
    vh = window.innerHeight,
    pad = 8;
  let [side, align = "start"] = String(placement).split("-");
  const fitsBelow = anchorRect.bottom + offset + panelRect.height <= vh - pad;
  const fitsAbove = anchorRect.top - offset - panelRect.height >= pad;
  // Flip only when the preferred side does NOT fit and the other one does.
  // Flipping whenever the other side has more room makes a panel near the middle
  // of the viewport jump sides as the page scrolls by a pixel.
  if (side === "bottom" && !fitsBelow && fitsAbove) side = "top";else if (side === "top" && !fitsAbove && fitsBelow) side = "bottom";
  let top, left;
  if (side === "top") top = anchorRect.top - offset - panelRect.height;else if (side === "bottom") top = anchorRect.bottom + offset;else top = align === "end" ? anchorRect.bottom - panelRect.height : anchorRect.top;
  if (side === "left") left = anchorRect.left - offset - panelRect.width;else if (side === "right") left = anchorRect.right + offset;else if (align === "end") left = anchorRect.right - panelRect.width;else if (align === "center") left = anchorRect.left + (anchorRect.width - panelRect.width) / 2;else left = anchorRect.left;

  // Clamp last, so a panel wider than its anchor stays on screen instead of
  // hanging off the edge in the name of alignment.
  left = Math.min(Math.max(pad, left), Math.max(pad, vw - panelRect.width - pad));
  top = Math.min(Math.max(pad, top), Math.max(pad, vh - panelRect.height - pad));
  return {
    top,
    left,
    side
  };
}

/**
 * The floating surface. Menu, Combobox, DatePicker and the filter panels are all
 * this plus their own contents — there is one shadow, one radius and one
 * dismissal behaviour in the system, not five.
 *
 * Controlled (`open` + `onOpenChange`) or uncontrolled. The trigger is cloned to
 * receive the ref, the click handler and `aria-expanded`, so a caller passes an
 * ordinary Button and gets correct semantics without wiring them.
 */
function Popover({
  trigger,
  open: openProp,
  defaultOpen = false,
  onOpenChange,
  placement = "bottom-start",
  offset = 6,
  matchWidth,
  label,
  role = "dialog",
  padded,
  triggerAria = true,
  className,
  children,
  ...rest
}) {
  const [uncontrolled, setUncontrolled] = React.useState(defaultOpen);
  const isControlled = openProp != null;
  const open = isControlled ? openProp : uncontrolled;
  const anchorRef = React.useRef(null);
  const panelRef = React.useRef(null);
  const uid = React.useId();

  // .lw-popover-anchor is display:contents so it never disturbs the trigger's
  // layout — a wrapper with a box stretches inside a Stack and shrinks a Cluster.
  // The cost is that the WRAPPER has no box either: getBoundingClientRect() on it
  // returns all zeros, which silently pins every panel to the viewport corner. So
  // always measure the trigger's own element. (Cloning a ref onto the trigger is
  // not an option — the components passed in here are plain function components
  // and do not forward one.)
  const anchorEl = () => {
    const w = anchorRef.current;
    return w && w.firstElementChild || w;
  };
  const setOpen = React.useCallback(next => {
    if (!isControlled) setUncontrolled(next);
    onOpenChange && onOpenChange(next);
  }, [isControlled, onOpenChange]);

  // Show/hide against the TOP LAYER. showPopover throws if the element is already
  // open, so both calls are guarded rather than fired optimistically.
  React.useEffect(() => {
    const el = panelRef.current;
    if (!el || typeof el.showPopover !== "function") return;
    const isOpen = el.matches(":popover-open");
    if (open && !isOpen) el.showPopover();
    if (!open && isOpen) el.hidePopover();
  }, [open]);

  // Position after paint, and again on any scroll or resize: a fixed panel does
  // not travel with an anchor inside a scrolling pane. Capture phase, because the
  // scroll may happen in an ancestor that does not bubble scroll events.
  React.useEffect(() => {
    if (!open) return;
    const reposition = () => {
      const anchor = anchorEl(),
        panel = panelRef.current;
      if (!anchor || !panel) return;
      const rect = anchor.getBoundingClientRect();
      // A zero-size anchor means the trigger has not laid out yet (or is display:none).
      // Positioning against it would clamp the panel to the viewport corner, so skip
      // this frame rather than paint the panel somewhere provably wrong.
      if (!rect.width && !rect.height) return;
      if (matchWidth) panel.style.minWidth = rect.width + "px";
      const pos = place(rect, panel.getBoundingClientRect(), placement, offset);
      panel.style.top = pos.top + "px";
      panel.style.left = pos.left + "px";
      panel.dataset.side = pos.side;
    };
    reposition();
    window.addEventListener("scroll", reposition, true);
    window.addEventListener("resize", reposition);
    return () => {
      window.removeEventListener("scroll", reposition, true);
      window.removeEventListener("resize", reposition);
    };
  }, [open, placement, offset, matchWidth]);

  // Dismissal is ours rather than popover="auto"'s, because auto light-dismisses
  // on pointerdown anywhere outside — including the trigger, which then reopens
  // the panel its own click just closed.
  React.useEffect(() => {
    if (!open) return;
    const onPointerDown = e => {
      const anchor = anchorRef.current,
        panel = panelRef.current;
      if (panel && panel.contains(e.target)) return;
      if (anchor && anchor.contains(e.target)) return;
      setOpen(false);
    };
    const onKeyDown = e => {
      if (e.key !== "Escape") return;
      e.stopPropagation();
      setOpen(false);
      const t = anchorEl();
      if (t && t.focus) t.focus({
        preventScroll: true
      });
    };
    document.addEventListener("pointerdown", onPointerDown, true);
    document.addEventListener("keydown", onKeyDown, true);
    return () => {
      document.removeEventListener("pointerdown", onPointerDown, true);
      document.removeEventListener("keydown", onKeyDown, true);
    };
  }, [open, setOpen]);
  const HASPOPUP = {
    menu: "menu",
    listbox: "listbox",
    grid: "grid",
    dialog: "dialog"
  };
  const triggerEl = React.isValidElement(trigger) ? React.cloneElement(trigger, {
    // A combobox input carries role="combobox" and its own aria-expanded /
    // aria-controls / aria-activedescendant. Cloning a second set onto the
    // field wrapper would announce two controls where there is one.
    "aria-expanded": triggerAria ? open : undefined,
    "aria-haspopup": triggerAria ? HASPOPUP[role] || "dialog" : undefined,
    "aria-controls": triggerAria && open ? uid : undefined,
    onClick: e => {
      trigger.props.onClick && trigger.props.onClick(e);
      if (!e.defaultPrevented) setOpen(!open);
    }
  }) : trigger;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "lw-popover-anchor",
    ref: anchorRef
  }, triggerEl), /*#__PURE__*/React.createElement("div", _extends({
    ref: panelRef,
    id: uid,
    popover: "manual",
    role: role,
    "aria-label": label,
    tabIndex: -1,
    className: cx("lw-popover", padded && "lw-popover-pad", className)
  }, rest), open && children));
}
Object.assign(__ds_scope, { Popover });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Popover.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Tooltip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** CSS-only, on a wrapper. For hints — never for content the user needs: it
 *  does not exist on touch and it cannot be read aloud on its own. */
function Tooltip({
  tip,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-tip", className),
    "data-tip": tip
  }, rest), children);
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Tooltip.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Avatar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Initials by default; an image only when there is one. Never a generated
 *  face — a placeholder person is a fabrication. */
function Avatar({
  name = "",
  src,
  size = "md",
  className,
  ...rest
}) {
  const initials = name.trim().split(/\s+/).slice(0, 2).map(w => w[0] || "").join("").toUpperCase();
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-avatar", size === "sm" && "lw-avatar-sm", size === "lg" && "lw-avatar-lg", className),
    title: name || undefined
  }, rest), src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: name
  }) : initials);
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The LeanWise button. Renders the shipped `.lw-btn` CSS — no styling of its
 * own — so the React and vanilla consumers can never drift apart.
 *
 * `cta` is the amber. One per view; the token lint enforces it.
 */
function Button({
  variant = "brand",
  size = "md",
  iconOnly = false,
  loading = false,
  disabled = false,
  as,
  className,
  onClick,
  children,
  ...rest
}) {
  const Tag = as || (rest.href ? "a" : "button");
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-btn", `lw-btn-${variant}`, size === "sm" && "lw-btn-sm", size === "lg" && "lw-btn-lg", iconOnly && "lw-btn-icon", className)
    // A loading button must stay focusable (so the ring is visible) but must
    // not fire twice — aria-disabled, not disabled.
    ,
    "data-loading": loading ? "true" : undefined,
    "aria-disabled": loading || disabled ? "true" : undefined,
    disabled: Tag === "button" ? disabled : undefined
    // aria-disabled keeps the ring but not the guard: an <a> or a loading
    // button still fires without this, which is how a double-submit ships.
    ,
    onClick: e => {
      if (loading || disabled) {
        e.preventDefault();
        e.stopPropagation();
        return;
      }
      onClick && onClick(e);
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Button.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Card.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The surface. `interactive` is not cosmetic — it adds the pointer, the focus
 * ring and the press state a clickable card legally needs, and switches the
 * element to a button unless you pass `href`.
 */
function Card({
  interactive = false,
  glow = false,
  selected,
  as,
  className,
  children,
  ...rest
}) {
  const Tag = as || (interactive ? rest.href ? "a" : "button" : "div");
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-card", interactive && "lw-card-interactive", glow && "lw-card-glow", className),
    "aria-selected": selected,
    type: Tag === "button" ? "button" : undefined
  }, rest), children);
}
function CardHead({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-card-head", className)
  }, rest), children);
}
function CardTitle({
  as: Tag = "h3",
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-card-title", className)
  }, rest), children);
}
function CardBody({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("p", _extends({
    className: cx("lw-card-body", className)
  }, rest), children);
}
function CardFoot({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-card-foot", className)
  }, rest), children);
}
Object.assign(__ds_scope, { Card, CardHead, CardTitle, CardBody, CardFoot });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Card.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Chip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Status atom. Reads a status family's soft tint + text tier, so it re-points
 *  on dark with no conditional. */
function Chip({
  tone = "brand",
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-chip", tone !== "brand" && `lw-chip-${tone}`, className)
  }, rest), children);
}
Object.assign(__ds_scope, { Chip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Chip.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Eyebrow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The signature motif: mono, uppercase, wide-tracked, with a cyan dot. */
function Eyebrow({
  as: Tag = "p",
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("lw-eyebrow", className)
  }, rest), children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The icon set. One 24-grid, one stroke weight, `currentColor` only — so an
 * icon inherits the ink of whatever it sits in and can never introduce a colour
 * the tokens do not know about.
 *
 * Paths, never a font: an icon font ships one glyph height for every icon and
 * fails visibly while the font loads. Sized in px because icons align to text,
 * not to the space scale.
 *
 * Decorative by default (`aria-hidden`). Pass `label` only when the icon is the
 * sole content of a control and nothing else names it.
 */
const ICONS = {
  sidebar: ["M4 5.5A2 2 0 0 1 6 3.5h12a2 2 0 0 1 2 2v13a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2z", "M10 3.5v17"],
  "sidebar-right": ["M4 5.5A2 2 0 0 1 6 3.5h12a2 2 0 0 1 2 2v13a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2z", "M14 3.5v17"],
  plus: ["M12 5v14", "M5 12h14"],
  paperclip: ["M13.4 6.6 7.7 12.3a3.2 3.2 0 0 0 4.5 4.5l6.4-6.4a5 5 0 0 0-7.1-7.1l-6.6 6.6a6.8 6.8 0 0 0 9.6 9.6l3.2-3.2"],
  filter: ["M4 6.5h16", "M7 12h10", "M10 17.5h4"],
  send: ["M12 19.5V5", "M6 11l6-6 6 6"],
  book: ["M19.5 3H7a2.5 2.5 0 0 0-2.5 2.5v13A2.5 2.5 0 0 1 7 16h12.5z", "M4.5 18.5A2.5 2.5 0 0 1 7 21h12.5v-5"],
  quote: ["M10.4 6.6C7.6 7.9 5.9 10.3 5.9 13.2v4.2h5.4v-5.1H8.6c0-1.7.7-3 2.1-3.9z", "M19.1 6.6c-2.8 1.3-4.5 3.7-4.5 6.6v4.2H20v-5.1h-2.7c0-1.7.7-3 2.1-3.9z"],
  list: ["M9 6.5h11", "M9 12h11", "M9 17.5h11", "M4.6 6.5h.01", "M4.6 12h.01", "M4.6 17.5h.01"],
  close: ["M6.5 6.5l11 11", "M17.5 6.5l-11 11"],
  copy: ["M9.5 8.5h9a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1h-9a1 1 0 0 1-1-1v-9a1 1 0 0 1 1-1z", "M5.5 15.5a1 1 0 0 1-1-1v-9a1 1 0 0 1 1-1h9a1 1 0 0 1 1 1"],
  retry: ["M19.5 12a7.5 7.5 0 1 1-2.2-5.3", "M19.5 4.5V9H15"],
  download: ["M12 4.5v10.5", "M7.5 11l4.5 4.5 4.5-4.5", "M5 19.5h14"],
  spark: ["M12 3.5l2.1 5.4 5.4 2.1-5.4 2.1L12 18.5l-2.1-5.4L4.5 11l5.4-2.1z"],
  layers: ["M12 3.5l8 4.3-8 4.3-8-4.3z", "M4 12.6l8 4.3 8-4.3"],
  check: ["M12 3.5a8.5 8.5 0 1 0 0 17 8.5 8.5 0 0 0 0-17", "M8.5 12.3l2.6 2.6 4.6-5.2"],
  users: ["M9 4.5a3.3 3.3 0 1 0 0 6.6 3.3 3.3 0 0 0 0-6.6", "M3.5 20a5.5 5.5 0 0 1 11 0", "M16 5.6a3.3 3.3 0 0 1 0 5.4", "M17.2 20a5.6 5.6 0 0 0-1.7-3.9"],
  settings: ["M12 9a3 3 0 1 0 0 6 3 3 0 0 0 0-6", "M12 3.5v2.2", "M12 18.3v2.2", "M5.2 7.6l1.9 1.1", "M16.9 15.3l1.9 1.1", "M5.2 16.4l1.9-1.1", "M16.9 8.7l1.9-1.1"],
  clock: ["M12 3.5a8.5 8.5 0 1 0 0 17 8.5 8.5 0 0 0 0-17", "M12 7.5V12l3.4 2"],
  search: ["M10.8 4.2a6.6 6.6 0 1 0 0 13.2 6.6 6.6 0 0 0 0-13.2", "M15.6 15.6l4.2 4.2"],
  key: ["M15.4 3.6a5 5 0 0 0-4.4 7.3L3.5 18.4V20.5h2.1l1-1v-1.8h1.8l1-1h1.8l1.6-1.6a5 5 0 1 0 1.6-9.5", "M17 7.6h.01"],
  database: ["M12 3.5c4 0 7.2 1.1 7.2 2.5S16 8.5 12 8.5 4.8 7.4 4.8 6S8 3.5 12 3.5", "M4.8 6v12c0 1.4 3.2 2.5 7.2 2.5s7.2-1.1 7.2-2.5V6", "M4.8 12c0 1.4 3.2 2.5 7.2 2.5s7.2-1.1 7.2-2.5"],
  shield: ["M12 3.2l7 2.6v5.4c0 4.2-2.8 7.5-7 9.6-4.2-2.1-7-5.4-7-9.6V5.8z", "M8.9 11.9l2.2 2.2 4-4.4"],
  code: ["M9 7.5L4.5 12 9 16.5", "M15 7.5L19.5 12 15 16.5"],
  webhook: ["M8.6 10.4a3.9 3.9 0 1 1 5.6 3.5", "M12 14a4 4 0 1 0 3.7 5.5", "M15.7 19.5H8.2", "M8.3 14.2A4 4 0 1 0 4.6 9"],
  alert: ["M12 3.8l8.5 15.2H3.5z", "M12 9.6v4.1", "M12 16.6h.01"],
  rocket: ["M13.5 4.6c3.2-1.1 5.9-.9 5.9-.9s.2 2.7-.9 5.9c-.9 2.6-3.7 5.4-6 7l-3.2-.5-2.3-2.3-.5-3.2c1.6-2.3 4.4-5.1 7-6z", "M14.8 8.9h.01", "M8.6 16.4l-3 3", "M6.2 12.6L4 13.4l1.6 1.6", "M11.4 17.8l.8-2.2 1.6 1.6"],
  "chevron-down": ["M6.5 9.5l5.5 6 5.5-6"],
  sun: ["M12 8.2a3.8 3.8 0 1 0 0 7.6 3.8 3.8 0 0 0 0-7.6", "M12 2.8v2.1", "M12 19.1v2.1", "M4.9 4.9l1.5 1.5", "M17.6 17.6l1.5 1.5", "M2.8 12h2.1", "M19.1 12h2.1", "M4.9 19.1l1.5-1.5", "M17.6 6.4l1.5-1.5"],
  moon: ["M20 14.4A8.4 8.4 0 0 1 9.6 4a8.5 8.5 0 1 0 10.4 10.4"],
  monitor: ["M4.5 5h15a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1z", "M9 20h6", "M12 16v4"],
  edit: ["M4.5 19.5h4L20 8a2.1 2.1 0 0 0-3-3L5.5 16.5z"],
  "chevron-left": ["M14.5 6.5l-6 5.5 6 5.5"],
  "chevron-right": ["M9.5 6.5l6 5.5-6 5.5"],
  "chevron-up": ["M6.5 14.5l5.5-6 5.5 6"],
  user: ["M12 4.6a3.6 3.6 0 1 0 0 7.2 3.6 3.6 0 0 0 0-7.2", "M5.2 20a6.8 6.8 0 0 1 13.6 0"],
  "arrow-up": ["M12 19.5V5", "M6 11l6-6 6 6"],
  "arrow-down": ["M12 4.5V19", "M18 13l-6 6-6-6"],
  "arrow-right": ["M4.5 12h15", "M13.5 6l6 6-6 6"],
  "arrow-left": ["M19.5 12h-15", "M10.5 6l-6 6 6 6"],
  trash: ["M5 7.5h14", "M9.5 7.5V5.6a1.1 1.1 0 0 1 1.1-1.1h2.8a1.1 1.1 0 0 1 1.1 1.1v1.9", "M6.9 7.5l.8 11.1a1.6 1.6 0 0 0 1.6 1.4h5.4a1.6 1.6 0 0 0 1.6-1.4l.8-11.1", "M10.4 11v5.4", "M13.6 11v5.4"],
  external: ["M14 4.5h5.5V10", "M19.5 4.5L11 13", "M17.5 14v4.5a1.5 1.5 0 0 1-1.5 1.5H6a1.5 1.5 0 0 1-1.5-1.5V8A1.5 1.5 0 0 1 6 6.5h4.5"],
  info: ["M12 3.5a8.5 8.5 0 1 0 0 17 8.5 8.5 0 0 0 0-17", "M12 11.2v5.2", "M12 7.7h.01"],
  more: ["M6 12h.01", "M12 12h.01", "M18 12h.01"],
  file: ["M13.5 3.5H7A1.5 1.5 0 0 0 5.5 5v14A1.5 1.5 0 0 0 7 20.5h10a1.5 1.5 0 0 0 1.5-1.5V8.5z", "M13.5 3.5v5h5"],
  chart: ["M4.5 19.5h15", "M7.6 19.5v-6.2", "M12 19.5V6.4", "M16.4 19.5v-4.1"],
  /* ---- Added for the control layer (Popover, Menu, Combobox, DataGrid,
     DatePicker, Upload, Notifications, AI review). One pass, not one at a time:
     Rule 8 ("name a glyph, never draw one") only holds while adding one is cheap. */
  calendar: ["M5 6.5h14a1 1 0 0 1 1 1v11a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1v-11a1 1 0 0 1 1-1z", "M4 10.5h16", "M8.5 4v4", "M15.5 4v4"],
  upload: ["M12 15.5V4.5", "M7.5 9L12 4.5 16.5 9", "M5 19.5h14"],
  /* A thumbtack seen head-on: a cap, a tapering shaft, a point. The first pass drew
     it at an angle, which at 16px read as an unidentifiable wedge — a glyph has to
     survive the size it is actually used at, and pin is used in a 12px column head. */
  pin: ["M8.5 4h7", "M10.5 4v6l-2.5 3h8l-2.5-3V4", "M12 13v7"],
  grip: ["M9 6h.01", "M15 6h.01", "M9 12h.01", "M15 12h.01", "M9 18h.01", "M15 18h.01"],
  columns: ["M4.5 5h15a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1z", "M9.5 5v14", "M15 5v14"],
  /* `check` is the CIRCLED check and predates this set; `checkmark` is the bare
     one a menu item and a checkbox need. Two glyphs, two names, no renaming — a
     rename here would silently repoint every existing consumer. */
  checkmark: ["M5 12.8l4.6 4.7L19 7.5"],
  minus: ["M5 12h14"],
  "x-circle": ["M12 3.5a8.5 8.5 0 1 0 0 17 8.5 8.5 0 0 0 0-17", "M9.2 9.2l5.6 5.6", "M14.8 9.2l-5.6 5.6"],
  "more-vertical": ["M12 6h.01", "M12 12h.01", "M12 18h.01"],
  "chevrons-up-down": ["M8 10l4-4 4 4", "M8 14l4 4 4-4"],
  /* Direction is never ONE cue (rule 6). The first pass distinguished asc from desc
     by line length alone — three bars getting shorter versus longer, which nobody
     reads as a direction at a glance and nothing reads in a 13px table header. Both
     now carry an arrow as well, so the sort direction survives being small. */
  "sort-asc": ["M4.5 7h7", "M4.5 12h5", "M4.5 17h3", "M17 18.5V7.5", "M14 10.5l3-3 3 3"],
  "sort-desc": ["M4.5 7h7", "M4.5 12h5", "M4.5 17h3", "M17 7.5v11", "M14 15.5l3 3 3-3"],
  eye: ["M2.8 12S6.5 5.8 12 5.8 21.2 12 21.2 12 17.5 18.2 12 18.2 2.8 12 2.8 12z", "M12 9.2a2.8 2.8 0 1 0 0 5.6 2.8 2.8 0 0 0 0-5.6"],
  "eye-off": ["M9.9 5.9A8 8 0 0 1 12 5.8c5.5 0 9.2 6.2 9.2 6.2a17 17 0 0 1-3 3.7", "M6.3 7.9A17 17 0 0 0 2.8 12S6.5 18.2 12 18.2a8.3 8.3 0 0 0 3.3-.7", "M10 10a2.8 2.8 0 0 0 3.9 3.9", "M4.5 4.5l15 15"],
  lock: ["M6.5 10.5h11a1 1 0 0 1 1 1v7a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1v-7a1 1 0 0 1 1-1z", "M8.5 10.5V8a3.5 3.5 0 0 1 7 0v2.5"],
  mail: ["M4.5 5.5h15a1 1 0 0 1 1 1v11a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1v-11a1 1 0 0 1 1-1z", "M4 7l8 5.5L20 7"],
  link: ["M10.5 13.5a4 4 0 0 0 5.7 0l2.6-2.6a4 4 0 0 0-5.7-5.7l-1.5 1.5", "M13.5 10.5a4 4 0 0 0-5.7 0l-2.6 2.6a4 4 0 0 0 5.7 5.7l1.5-1.5"],
  image: ["M4.5 4.5h15a1 1 0 0 1 1 1v13a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1v-13a1 1 0 0 1 1-1z", "M9 10.2a1.7 1.7 0 1 0 0-3.4 1.7 1.7 0 0 0 0 3.4", "M20.5 15.5l-4.8-4.8L5 19.5"],
  folder: ["M3.5 6.5a1 1 0 0 1 1-1h4.3l2 2.5h8.7a1 1 0 0 1 1 1v9.5a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1z"],
  star: ["M12 3.8l2.6 5.3 5.8.8-4.2 4.1 1 5.8-5.2-2.8-5.2 2.8 1-5.8-4.2-4.1 5.8-.8z"],
  bell: ["M18 9.5a6 6 0 1 0-12 0c0 5-2 6.5-2 6.5h16s-2-1.5-2-6.5", "M13.7 19.5a2 2 0 0 1-3.4 0"],
  inbox: ["M6.3 5h11.4a1 1 0 0 1 .95.68l1.85 5.5a1 1 0 0 1 .05.32v6.5a1 1 0 0 1-1 1h-15a1 1 0 0 1-1-1V11.5a1 1 0 0 1 .05-.32l1.85-5.5A1 1 0 0 1 6.3 5z", "M3.6 12.5h4.4l1.5 3h5l1.5-3h4.4"],
  play: ["M8 5.5l10 6.5-10 6.5z"],
  pause: ["M9 5.5v13", "M15 5.5v13"],
  mic: ["M12 3.5a2.8 2.8 0 0 1 2.8 2.8v5.4a2.8 2.8 0 0 1-5.6 0V6.3A2.8 2.8 0 0 1 12 3.5", "M5.5 11a6.5 6.5 0 0 0 13 0", "M12 17.5v3"],
  "mic-off": ["M9.2 6.3a2.8 2.8 0 0 1 5.6 0v5.4a2.8 2.8 0 0 1-.3 1.3", "M14.5 14.9a2.8 2.8 0 0 1-5.3-1.2V9.8", "M5.5 11a6.5 6.5 0 0 0 10.4 5.2", "M18.5 11v.6", "M12 17.5v3", "M4.5 4.5l15 15"],
  "thumbs-up": ["M7.5 20V9.5l4-6a2 2 0 0 1 3 2.2L13.8 9h4.7a2 2 0 0 1 2 2.4l-1.4 6.5a2 2 0 0 1-2 1.6z", "M7.5 9.5H4.8a1 1 0 0 0-1 1V19a1 1 0 0 0 1 1h2.7"],
  "thumbs-down": ["M16.5 4v10.5l-4 6a2 2 0 0 1-3-2.2l.7-3.3H5.5a2 2 0 0 1-2-2.4l1.4-6.5A2 2 0 0 1 6.9 4z", "M16.5 14.5h2.7a1 1 0 0 0 1-1V5a1 1 0 0 0-1-1h-2.7"],
  maximize: ["M9 4.5H4.5V9", "M15 4.5h4.5V9", "M15 19.5h4.5V15", "M9 19.5H4.5V15"],
  minimize: ["M4.5 9H9V4.5", "M19.5 9H15V4.5", "M19.5 15H15v4.5", "M4.5 15H9v4.5"],
  undo: ["M4.5 12a7.5 7.5 0 1 0 2.2-5.3", "M4.5 4.5V9H9"],
  help: ["M12 3.5a8.5 8.5 0 1 0 0 17 8.5 8.5 0 0 0 0-17", "M9.6 9.4a2.5 2.5 0 0 1 4.9.6c0 1.7-2.5 2.5-2.5 2.5", "M12 16.5h.01"]
};

/* Drawn when a name is not in the set. Deliberately generic — it must read as
   "a glyph is missing here", never as a real icon someone might ship. Rendering
   NOTHING was the old behaviour and it was worse: an empty but space-occupying
   slot silently breaks an icon column's rhythm, and the gap is invisible in
   review while the console warning scrolls past. */
const UNKNOWN = ["M5.5 5.5h13v13h-13z", "M9.4 9.4l5.2 5.2", "M14.6 9.4l-5.2 5.2"];
function Icon({
  name,
  size = 16,
  strokeWidth = 1.6,
  label,
  className,
  style,
  ...rest
}) {
  const known = ICONS[name];
  // Loud AND visible. The warning is what caught nine silently-missing glyphs, so
  // it stays; but the slot now draws UNKNOWN rather than returning null, because
  // an empty-but-space-occupying slot breaks an icon column's rhythm in a way
  // that is easy to miss in review while the warning scrolls past.
  if (!known && typeof console !== "undefined") {
    console.warn(`Icon: no glyph named "${name}". Known names: ${Object.keys(ICONS).join(", ")}`);
  }
  const paths = known || UNKNOWN;
  // The size goes inline, not just on the presentation attributes: any class rule
  // (`.lw-btn svg { width: 16px }`) outranks a presentation attribute, which
  // silently made `size` inert inside buttons. Inline keeps `size` authoritative.
  return /*#__PURE__*/React.createElement("svg", _extends({
    className: cx("lw-icon", className),
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    style: {
      width: size,
      height: size,
      ...style
    },
    fill: "none",
    stroke: "currentColor",
    strokeWidth: strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    role: label ? "img" : undefined,
    "aria-label": label,
    "aria-hidden": label ? undefined : "true",
    "data-unknown": known ? undefined : "true"
  }, rest), paths.map((d, i) => /*#__PURE__*/React.createElement("path", {
    key: i,
    d: d
  })));
}
/** Every glyph name, in declaration order. Exported capitalised as well, so it
 *  reaches window.<Namespace> — a set nobody can enumerate is a set people
 *  guess at, and a guessed name renders nothing. */
const iconNames = Object.keys(ICONS);
const IconNames = iconNames;
Object.assign(__ds_scope, { Icon, iconNames, IconNames });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Icon.jsx", error: String((e && e.message) || e) }); }

// components/ai/Artifact.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The side surface that holds generated output. Versioned, with a way back —
 * an artifact you cannot revert is a document the model can overwrite.
 *
 * The system rule this encodes: an AI surface is never the ONLY path to an
 * outcome. `onEdit` is not optional decoration; it is the manual equivalent of
 * whatever produced this.
 */
function Artifact({
  title,
  version,
  versionCount,
  onPrevVersion,
  onNextVersion,
  onRevert,
  onEdit,
  actions,
  className,
  children,
  ...rest
}) {
  const canPrev = version > 1;
  const canNext = versionCount != null && version < versionCount;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-artifact", className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-artifact-head"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "file",
    size: 15,
    style: {
      color: "var(--lw-fg-subtle)",
      flex: "none"
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-artifact-title"
  }, title), version != null && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Previous version",
    disabled: !canPrev,
    onClick: onPrevVersion
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-left",
    size: 15
  })), /*#__PURE__*/React.createElement("span", {
    className: "lw-artifact-ver"
  }, "v", version, versionCount ? " / " + versionCount : ""), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Next version",
    disabled: !canNext,
    onClick: onNextVersion
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-right",
    size: 15
  })))), /*#__PURE__*/React.createElement("div", {
    className: "lw-artifact-body"
  }, children), (onRevert || onEdit || actions) && /*#__PURE__*/React.createElement("div", {
    className: "lw-artifact-foot"
  }, onEdit && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-btn lw-btn-ghost lw-btn-sm",
    onClick: onEdit
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "edit",
    size: 14
  }), "Edit manually"), onRevert && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-btn lw-btn-ghost lw-btn-sm",
    onClick: onRevert
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "undo",
    size: 14
  }), "Revert"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), actions));
}
Object.assign(__ds_scope, { Artifact });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/Artifact.jsx", error: String((e && e.message) || e) }); }

// components/ai/DiffReview.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const SIGN = {
  add: "+",
  del: "−",
  mod: "~"
};

/**
 * Accept or reject a model's edits, hunk by hunk — the most under-designed
 * surface in AI products, and the one that decides whether people trust the
 * feature.
 *
 * The gutter carries the sign as well as the ground, so the diff reads in
 * greyscale and to a colour-blind reader (README rule 6), and the ground tokens
 * are GROUNDS: the text on top stays `--lw-fg`, because green-on-green is
 * unreadable at exactly the moment it matters.
 */
function DiffReview({
  hunks = [],
  decisions = {},
  onDecide,
  onAcceptAll,
  onRejectAll,
  label = "Proposed changes",
  className,
  ...rest
}) {
  const pending = hunks.filter(h => !decisions[h.id]).length;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-diff", className),
    role: "group",
    "aria-label": label
  }, rest), hunks.map(h => {
    const d = decisions[h.id];
    return /*#__PURE__*/React.createElement("div", {
      key: h.id,
      className: "lw-diff-hunk",
      "data-decision": d
    }, /*#__PURE__*/React.createElement("div", {
      className: "lw-diff-head"
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "file",
      size: 14,
      style: {
        color: "var(--lw-fg-subtle)",
        flex: "none"
      }
    }), /*#__PURE__*/React.createElement("span", {
      className: "lw-diff-file"
    }, h.file, h.range ? " · " + h.range : "")), /*#__PURE__*/React.createElement("div", {
      className: "lw-diff-lines"
    }, h.lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      className: "lw-diff-line",
      "data-kind": l.kind
    }, /*#__PURE__*/React.createElement("span", {
      className: "n"
    }, l.n ?? ""), /*#__PURE__*/React.createElement("span", {
      className: "s",
      "aria-hidden": "true"
    }, SIGN[l.kind] || ""), /*#__PURE__*/React.createElement("span", {
      className: "t"
    }, l.kind && /*#__PURE__*/React.createElement("span", {
      className: "lw-sr-only"
    }, l.kind === "add" ? "added: " : l.kind === "del" ? "removed: " : "changed: "), l.text)))), /*#__PURE__*/React.createElement("div", {
      className: "lw-diff-foot"
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-diff-state"
    }, d === "accepted" ? "Accepted" : d === "rejected" ? "Rejected" : h.note || ""), d ? /*#__PURE__*/React.createElement(__ds_scope.Button, {
      size: "sm",
      variant: "ghost",
      onClick: () => onDecide && onDecide(h.id, null)
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "undo",
      size: 14
    }), "Undo") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.Button, {
      size: "sm",
      variant: "ghost",
      onClick: () => onDecide && onDecide(h.id, "rejected")
    }, "Reject"), /*#__PURE__*/React.createElement(__ds_scope.Button, {
      size: "sm",
      onClick: () => onDecide && onDecide(h.id, "accepted")
    }, "Accept"))));
  }), hunks.length > 1 && /*#__PURE__*/React.createElement("div", {
    className: "lw-diff-foot"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lw-diff-state",
    "aria-live": "polite"
  }, pending ? pending + " of " + hunks.length + " still to review" : "All " + hunks.length + " reviewed"), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    size: "sm",
    variant: "ghost",
    onClick: onRejectAll,
    disabled: !pending
  }, "Reject all"), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    size: "sm",
    onClick: onAcceptAll,
    disabled: !pending
  }, "Accept all")));
}
Object.assign(__ds_scope, { DiffReview });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/DiffReview.jsx", error: String((e && e.message) || e) }); }

// components/ai/Feedback.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Thumbs, and a way to say what was wrong. Trivial to build, always retrofitted
 * badly, and it is the thing that makes evals possible later — so it ships now
 * rather than after the model is already in production.
 *
 * A rating with no correction path collects a number nobody can act on, which
 * is why `onComment` exists and why the down state opens it by default.
 */
function Feedback({
  value,
  onChange,
  onComment,
  commentPlaceholder = "What was wrong?",
  note,
  className,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  const [text, setText] = React.useState("");
  const set = v => {
    const next = value === v ? null : v;
    onChange && onChange(next);
    if (next === "down" && onComment) setOpen(true);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx(className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-feedback"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Helpful",
    "aria-pressed": value === "up",
    onClick: () => set("up")
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "thumbs-up",
    size: 15
  })), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Not helpful",
    "aria-pressed": value === "down",
    onClick: () => set("down")
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "thumbs-down",
    size: 15
  })), note && /*#__PURE__*/React.createElement("span", {
    className: "lw-feedback-note"
  }, note)), open && onComment && /*#__PURE__*/React.createElement("form", {
    style: {
      display: "grid",
      gap: "var(--lw-space-8)",
      marginTop: "var(--lw-space-8)"
    },
    onSubmit: e => {
      e.preventDefault();
      onComment(text);
      setOpen(false);
      setText("");
    }
  }, /*#__PURE__*/React.createElement("textarea", {
    className: "lw-textarea",
    rows: 2,
    value: text,
    autoFocus: true,
    "aria-label": commentPlaceholder,
    placeholder: commentPlaceholder,
    onChange: e => setText(e.target.value)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--lw-space-8)",
      justifyContent: "flex-end"
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-btn lw-btn-ghost lw-btn-sm",
    onClick: () => setOpen(false)
  }, "Cancel"), /*#__PURE__*/React.createElement("button", {
    type: "submit",
    className: "lw-btn lw-btn-sm",
    disabled: !text.trim()
  }, "Send"))));
}
Object.assign(__ds_scope, { Feedback });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/Feedback.jsx", error: String((e && e.message) || e) }); }

// components/ai/Message.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * One turn. The assistant sits left, full measure, with a brand avatar; the
 * user's turn mirrors to the right in a bounded bubble. The asymmetry is the
 * point: a reader scanning a long session finds their own questions by shape,
 * while the answer keeps the line length that citation-dense prose needs.
 *
 * The badge is a ROLE marker, not an identity: a sparkle for the assistant and a
 * person glyph for the user. Initials would imply which teammate asked, which a
 * single-user thread cannot tell you. Pass `avatar` to override.
 *
 * `streaming` shows the caret. It must go off the moment the stream ends, or
 * the UI claims to still be thinking.
 */
// A sparkle for the assistant: the generated-answer mark users already read as
// "a model wrote this". Both glyphs come from Icon rather than being redrawn
// here — a second copy of a path is a second thing to keep in step.
function Message({
  role = "ai",
  who,
  avatar,
  streaming = false,
  footer,
  className,
  children,
  ...rest
}) {
  const name = who || (role === "ai" ? "LeanWise" : "You");
  const glyph = avatar || /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: role === "ai" ? "spark" : "user",
    size: role === "ai" ? 19 : 16
  });
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-msg", role, className),
    "data-streaming": streaming ? "true" : undefined
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "lw-msg-avatar",
    "aria-hidden": "true"
  }, glyph), /*#__PURE__*/React.createElement("div", {
    className: "lw-msg-main"
  }, /*#__PURE__*/React.createElement("span", {
    className: "who"
  }, name), /*#__PURE__*/React.createElement("div", {
    className: "body"
  }, children, footer)));
}
Object.assign(__ds_scope, { Message });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/Message.jsx", error: String((e && e.message) || e) }); }

// components/ai/ToolCall.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const fmt = v => typeof v === "string" ? v : JSON.stringify(v, null, 2);

/**
 * One tool invocation. `AgentTrace` shows THAT a step ran; this shows what it
 * did — arguments in, result out, how long it took. Pair them: the trace is the
 * spine, the tool calls are the evidence hanging off it.
 *
 * Collapsed by default, because an argument blob is something a user opens when
 * the answer looks wrong, not something to read on every turn.
 */
function ToolCall({
  name,
  summary,
  args,
  result,
  error,
  state = "ok",
  duration,
  defaultOpen,
  className,
  ...rest
}) {
  const [open, setOpen] = React.useState(!!defaultOpen);
  const uid = React.useId();
  const st = error ? "error" : state;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-tool", className),
    "data-state": st
  }, rest), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-tool-head",
    "aria-expanded": open,
    "aria-controls": uid,
    onClick: () => setOpen(o => !o)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: open ? "chevron-down" : "chevron-right",
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-tool-dot",
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-tool-name"
  }, name), /*#__PURE__*/React.createElement("span", {
    className: "lw-tool-sum"
  }, summary), duration != null && /*#__PURE__*/React.createElement("span", {
    className: "lw-tool-dur"
  }, duration, "ms"), /*#__PURE__*/React.createElement("span", {
    className: "lw-sr-only"
  }, st === "running" ? "running" : st === "error" ? "failed" : st === "pending" ? "pending" : "succeeded")), open && /*#__PURE__*/React.createElement("div", {
    className: "lw-tool-body",
    id: uid
  }, args != null && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "arguments"), /*#__PURE__*/React.createElement("pre", null, fmt(args))), error ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "error"), /*#__PURE__*/React.createElement("pre", {
    style: {
      color: "var(--lw-danger-on)"
    }
  }, fmt(error))) : result != null && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, "result"), /*#__PURE__*/React.createElement("pre", null, fmt(result)))));
}
Object.assign(__ds_scope, { ToolCall });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/ToolCall.jsx", error: String((e && e.message) || e) }); }

// components/data/ActivityFeed.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* Relative until it stops being useful. "3 days ago" is worse than a date the
   moment the user needs to correlate it with anything else. */
function timeAgo(when, now = Date.now()) {
  const t = when instanceof Date ? when.getTime() : new Date(when).getTime();
  const s = Math.max(0, (now - t) / 1000);
  if (s < 60) return "just now";
  if (s < 3600) return Math.floor(s / 60) + "m ago";
  if (s < 86400) return Math.floor(s / 3600) + "h ago";
  if (s < 86400 * 3) return Math.floor(s / 86400) + "d ago";
  return new Intl.DateTimeFormat(undefined, {
    day: "numeric",
    month: "short"
  }).format(t);
}
const bucket = (when, now) => {
  const d = new Date(when),
    n = new Date(now);
  const days = Math.floor((new Date(n.getFullYear(), n.getMonth(), n.getDate()) - new Date(d.getFullYear(), d.getMonth(), d.getDate())) / 86400000);
  return days <= 0 ? "Today" : days === 1 ? "Yesterday" : days < 7 ? "This week" : "Earlier";
};

/**
 * Notifications and activity, one component — they are the same list with a
 * different verb. Unread is a DOT plus weight, never a tint alone: a row that is
 * only slightly bluer than its neighbour is not a state anyone reads.
 *
 * Items are grouped by day bucket, because 40 undifferentiated rows with a
 * relative timestamp each is a list nobody scans.
 */
function ActivityFeed({
  items = [],
  onItemClick,
  grouped = true,
  now = Date.now(),
  label = "Activity",
  className,
  ...rest
}) {
  const groups = [];
  items.forEach(it => {
    const g = grouped && it.when ? bucket(it.when, now) : null;
    const last = groups[groups.length - 1];
    if (last && last.name === g) last.items.push(it);else groups.push({
      name: g,
      items: [it]
    });
  });
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-feed", className),
    role: "group",
    "aria-label": label
  }, rest), groups.map((g, gi) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: g.name ?? gi
  }, g.name && /*#__PURE__*/React.createElement("div", {
    className: "lw-feed-group"
  }, g.name), g.items.map((it, i) => {
    const Tag = it.href ? "a" : onItemClick || it.onClick ? "button" : "div";
    return /*#__PURE__*/React.createElement(Tag, {
      key: it.id ?? gi + "-" + i,
      className: "lw-feed-item",
      href: it.href || undefined,
      type: Tag === "button" ? "button" : undefined,
      "data-unread": it.unread ? "true" : undefined,
      "data-tone": it.tone,
      onClick: Tag === "div" ? undefined : () => it.onClick ? it.onClick(it) : onItemClick && onItemClick(it)
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      className: "lw-feed-ic"
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: it.icon,
      size: 15
    })), /*#__PURE__*/React.createElement("span", {
      className: "lw-feed-main"
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-feed-title"
    }, it.title), /*#__PURE__*/React.createElement("span", {
      className: "lw-feed-meta"
    }, it.when ? timeAgo(it.when, now) : null, it.meta ? (it.when ? " · " : "") + it.meta : "")), it.unread && /*#__PURE__*/React.createElement("span", {
      className: "lw-sr-only"
    }, "Unread"));
  }))));
}
Object.assign(__ds_scope, { timeAgo, ActivityFeed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ActivityFeed.jsx", error: String((e && e.message) || e) }); }

// components/data/CodeBlock.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Takes pre-highlighted HTML (tok-* spans) or raw code. Never highlights in
 *  the browser — that is the server's job and a runtime cost users pay twice.
 *
 *  The copy control is on by default whenever raw `code` is present: a snippet
 *  a reader has to select by hand is a snippet they mis-copy. It confirms in
 *  place rather than raising a toast — the feedback belongs where the click was. */
function CodeBlock({
  code,
  html,
  filename,
  lang,
  copy = true,
  className,
  ...rest
}) {
  const [copied, setCopied] = React.useState(false);
  const canCopy = copy && typeof code === "string" && code.length > 0;
  React.useEffect(() => {
    if (!copied) return;
    const t = setTimeout(() => setCopied(false), 1600);
    return () => clearTimeout(t);
  }, [copied]);
  const onCopy = () => {
    try {
      navigator.clipboard.writeText(code).then(() => setCopied(true), () => {});
    } catch (e) {}
  };
  return /*#__PURE__*/React.createElement("figure", _extends({
    className: cx("lw-code", className)
  }, rest), (filename || lang || canCopy) && /*#__PURE__*/React.createElement("figcaption", {
    className: "lw-code-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "fn"
  }, filename), /*#__PURE__*/React.createElement("span", {
    className: "end"
  }, lang && /*#__PURE__*/React.createElement("span", {
    className: "lang"
  }, lang), canCopy && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    onClick: onCopy,
    "aria-label": copied ? "Copied" : "Copy code",
    title: copied ? "Copied" : "Copy code"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: copied ? "check" : "copy",
    size: 15
  })))), /*#__PURE__*/React.createElement("pre", null, /*#__PURE__*/React.createElement("code", {
    dangerouslySetInnerHTML: html ? {
      __html: html
    } : undefined
  }, html ? undefined : code)));
}
Object.assign(__ds_scope, { CodeBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/CodeBlock.jsx", error: String((e && e.message) || e) }); }

// components/data/DataGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The data grid — sticky header, resizable and pinnable columns, bulk selection
 * and optional row windowing.
 *
 * It is deliberately NOT an extension of `Table`. A grid owns its own scroll
 * box and a pile of machinery a static table never needs; merging them would
 * make every simple table pay for it. Reach for `Table` first.
 */
function DataGrid({
  columns = [],
  rows = [],
  rowKey = (r, i) => r.id ?? i,
  sort,
  onSort,
  selectable,
  selected = [],
  onSelectionChange,
  height = 420,
  rowHeight = 44,
  virtualize,
  overscan = 8,
  onRowClick,
  empty = "No rows",
  selectionActions,
  label = "Data grid",
  className,
  ...rest
}) {
  const [widths, setWidths] = React.useState(() => columns.map(c => c.width || 160));
  const [scrollTop, setScrollTop] = React.useState(0);
  const scrollRef = React.useRef(null);
  const drag = React.useRef(null);
  React.useEffect(() => {
    setWidths(columns.map((c, i) => widths[i] || c.width || 160));
  }, [columns.length]);
  const selSet = React.useMemo(() => new Set(selected), [selected]);
  const allOn = rows.length > 0 && rows.every((r, i) => selSet.has(rowKey(r, i)));
  const someOn = !allOn && rows.some((r, i) => selSet.has(rowKey(r, i)));
  const toggleAll = () => onSelectionChange && onSelectionChange(allOn ? [] : rows.map(rowKey));
  const toggleRow = k => {
    if (!onSelectionChange) return;
    const next = new Set(selSet);
    next.has(k) ? next.delete(k) : next.add(k);
    onSelectionChange(Array.from(next));
  };

  // Pinned columns stack, so each one's offset is the SUM of the widths before
  // it — a fixed left per column only works while there is exactly one.
  const pinLefts = React.useMemo(() => {
    let acc = selectable ? 44 : 0;
    return columns.map((c, i) => {
      if (!c.pin) return null;
      const l = acc;
      acc += widths[i] || 160;
      return l;
    });
  }, [columns, widths, selectable]);
  const lastPin = columns.reduce((last, c, i) => c.pin ? i : last, -1);
  const onResizeDown = (i, e) => {
    e.preventDefault();
    drag.current = {
      i,
      x: e.clientX,
      w: widths[i] || 160
    };
    const move = ev => {
      const d = drag.current;
      if (!d) return;
      const min = columns[d.i].minWidth || 72;
      setWidths(w => w.map((v, n) => n === d.i ? Math.max(min, d.w + ev.clientX - d.x) : v));
    };
    const up = () => {
      drag.current = null;
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", up);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  // The handle is also a real button, so a keyboard user can size a column.
  const onResizeKey = (i, e) => {
    const d = e.key === "ArrowRight" ? 16 : e.key === "ArrowLeft" ? -16 : 0;
    if (!d) return;
    e.preventDefault();
    const min = columns[i].minWidth || 72;
    setWidths(w => w.map((v, n) => n === i ? Math.max(min, (v || 160) + d) : v));
  };
  const win = virtualize && rows.length * rowHeight > height;
  const start = win ? Math.max(0, Math.floor(scrollTop / rowHeight) - overscan) : 0;
  const visibleCount = win ? Math.ceil(height / rowHeight) + overscan * 2 : rows.length;
  const slice = win ? rows.slice(start, start + visibleCount) : rows;
  const padTop = win ? start * rowHeight : 0;
  const padBottom = win ? Math.max(0, (rows.length - start - slice.length) * rowHeight) : 0;
  const total = (selectable ? 44 : 0) + widths.reduce((s, w) => s + (w || 160), 0);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-dgrid", className)
  }, rest), selectable && selSet.size > 0 && /*#__PURE__*/React.createElement("div", {
    className: "lw-dgrid-selbar"
  }, /*#__PURE__*/React.createElement("span", {
    className: "count"
  }, selSet.size), /*#__PURE__*/React.createElement("span", null, "selected"), /*#__PURE__*/React.createElement("span", {
    className: "lw-dgrid-selbar-spacer"
  }), selectionActions, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-filter-clear",
    onClick: () => onSelectionChange && onSelectionChange([])
  }, "Clear")), /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    className: "lw-dgrid-scroll",
    style: {
      maxHeight: height
    },
    onScroll: win ? e => setScrollTop(e.currentTarget.scrollTop) : undefined
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      minWidth: total
    },
    "aria-label": label,
    "aria-rowcount": rows.length
  }, /*#__PURE__*/React.createElement("colgroup", null, selectable && /*#__PURE__*/React.createElement("col", {
    style: {
      width: 44
    }
  }), columns.map((c, i) => /*#__PURE__*/React.createElement("col", {
    key: c.key,
    style: {
      width: widths[i] || 160
    }
  }))), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, selectable && /*#__PURE__*/React.createElement("th", {
    "data-pin": "true",
    style: {
      left: 0
    },
    scope: "col"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lw-dgrid-check"
  }, /*#__PURE__*/React.createElement("label", {
    className: "lw-check",
    style: {
      gap: 0
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: allOn,
    ref: el => {
      if (el) el.indeterminate = someOn;
    },
    onChange: toggleAll,
    "aria-label": allOn ? "Clear selection" : "Select all rows"
  }), /*#__PURE__*/React.createElement("span", {
    className: "box"
  })))), columns.map((c, i) => {
    const dir = sort && sort.key === c.key ? sort.dir : null;
    return /*#__PURE__*/React.createElement("th", {
      key: c.key,
      scope: "col",
      className: cx(c.num && "num"),
      "data-pin": c.pin ? "true" : undefined,
      "data-pin-last": c.pin && i === lastPin ? "true" : undefined,
      style: c.pin ? {
        left: pinLefts[i]
      } : undefined,
      "aria-sort": dir ? dir === "asc" ? "ascending" : "descending" : undefined
    }, c.sortable && onSort ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "lw-dgrid-sort",
      onClick: () => onSort({
        key: c.key,
        dir: dir === "asc" ? "desc" : "asc"
      })
    }, c.header, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: dir === "asc" ? "sort-asc" : dir === "desc" ? "sort-desc" : "chevrons-up-down",
      size: 13
    })) : c.header, c.resizable !== false && /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "lw-dgrid-resize",
      "aria-label": "Resize " + (typeof c.header === "string" ? c.header : c.key),
      onPointerDown: e => onResizeDown(i, e),
      onKeyDown: e => onResizeKey(i, e)
    }));
  }))), /*#__PURE__*/React.createElement("tbody", null, padTop > 0 && /*#__PURE__*/React.createElement("tr", {
    "aria-hidden": "true",
    style: {
      height: padTop
    }
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: columns.length + (selectable ? 1 : 0),
    style: {
      padding: 0,
      border: 0
    }
  })), slice.map((r, n) => {
    const i = start + n;
    const k = rowKey(r, i);
    const on = selSet.has(k);
    return /*#__PURE__*/React.createElement("tr", {
      key: k,
      "aria-selected": on || undefined,
      "aria-rowindex": i + 2,
      style: {
        height: rowHeight,
        cursor: onRowClick ? "pointer" : undefined
      },
      onClick: onRowClick ? () => onRowClick(r, i) : undefined
    }, selectable && /*#__PURE__*/React.createElement("td", {
      "data-pin": "true",
      style: {
        left: 0
      },
      onClick: e => e.stopPropagation()
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-dgrid-check"
    }, /*#__PURE__*/React.createElement("label", {
      className: "lw-check",
      style: {
        gap: 0
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      checked: on,
      onChange: () => toggleRow(k),
      "aria-label": "Select row " + (i + 1)
    }), /*#__PURE__*/React.createElement("span", {
      className: "box"
    })))), columns.map((c, ci) => /*#__PURE__*/React.createElement("td", {
      key: c.key,
      className: cx(c.num && "num"),
      "data-pin": c.pin ? "true" : undefined,
      "data-pin-last": c.pin && ci === lastPin ? "true" : undefined,
      style: c.pin ? {
        left: pinLefts[ci]
      } : undefined
    }, c.render ? c.render(r, i) : r[c.key])));
  }), padBottom > 0 && /*#__PURE__*/React.createElement("tr", {
    "aria-hidden": "true",
    style: {
      height: padBottom
    }
  }, /*#__PURE__*/React.createElement("td", {
    colSpan: columns.length + (selectable ? 1 : 0),
    style: {
      padding: 0,
      border: 0
    }
  })))), !rows.length && /*#__PURE__*/React.createElement("div", {
    className: "lw-dgrid-empty"
  }, empty)));
}
Object.assign(__ds_scope, { DataGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DataGrid.jsx", error: String((e && e.message) || e) }); }

// components/data/EmptyState.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** The screen a product is judged by, and the one that gets left as "No data".
 *  Title, one line of why, one action.
 *
 *  `icon` names a glyph from the set — the same drawing an empty table's toolbar
 *  uses, so the empty state looks like the feature rather than like a gap.
 *  `glyph` still takes arbitrary content for the rare mono mark. */
function EmptyState({
  icon,
  glyph,
  title,
  description,
  action,
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-empty", className)
  }, rest), (icon || glyph) && /*#__PURE__*/React.createElement("span", {
    className: "glyph",
    "aria-hidden": "true"
  }, icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 22
  }) : glyph), /*#__PURE__*/React.createElement("span", {
    className: "t"
  }, title), description && /*#__PURE__*/React.createElement("span", {
    className: "s"
  }, description), action || children);
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/data/FilterBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The applied-filter row. Filters are shown as REMOVABLE chips, never only as
 * state inside a panel the user has to reopen to read — a filter you cannot see
 * is a filter you forget you set, and then the empty result looks like a broken
 * product rather than a narrow query.
 *
 * `clear all` appears from two filters up: with one, removing it IS clear all.
 */
function FilterBar({
  filters = [],
  onRemove,
  onClear,
  className,
  children,
  ...rest
}) {
  if (!filters.length && !children) return null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-filters", className),
    role: "group",
    "aria-label": "Applied filters"
  }, rest), children, filters.map(f => /*#__PURE__*/React.createElement("span", {
    key: f.id ?? f.key + ":" + f.value,
    className: "lw-filter-chip"
  }, f.key && /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, f.key), /*#__PURE__*/React.createElement("span", null, f.label ?? f.value), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Remove filter " + (f.key ? f.key + " " : "") + (f.label ?? f.value),
    onClick: () => onRemove && onRemove(f)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 11
  })))), filters.length > 1 && onClear && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-filter-clear",
    onClick: onClear
  }, "Clear all"));
}

/** The row above a list: search, filters, actions. A flex row with a growing
 *  slot — it owns the rhythm so five screens do not each invent their own. */
function Toolbar({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-toolbar", className)
  }, rest), children);
}
Object.assign(__ds_scope, { FilterBar, Toolbar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/FilterBar.jsx", error: String((e && e.message) || e) }); }

// components/data/KpiTile.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * One number, its label, at most one delta. Anything more is a Card.
 *
 * `direction` is which way the number moved (the arrow). `tone` is whether that
 * is good (the ink). They agree for revenue and disagree for latency, so they
 * are two props — fusing them paints every latency improvement red. Tone
 * defaults to direction when you do not say otherwise.
 *
 * The delta sits on the number's baseline to its RIGHT, not under it: the two
 * are one statement — "18.4k, up 12.1%" — and stacking them reads as two
 * separate figures. `icon` marks the tile's subject and sits opposite the
 * label, where it identifies the tile without competing with the number.
 *
 * `accent` tints the icon by SUBJECT — what the tile measures — and is a
 * different fact from `tone`, which judges how the number moved. Defaulting the
 * accent to the tone would paint three of four tiles green on a healthy
 * dashboard and say nothing the delta has not already said. Failures are the
 * danger family because failures are what they are, whichever way the count went.
 *
 * The chip's class sits on a WRAPPER, never on the Icon itself. A class shared
 * with the svg would be styled by a box rule the svg's own inline size always
 * outranks — so any mismatch between this file and the stylesheet would paint a
 * tinted 16px square rather than falling back to a plain glyph.
 */
function KpiTile({
  label,
  value,
  icon,
  accent = "brand",
  delta,
  direction,
  tone,
  note,
  className,
  ...rest
}) {
  const ink = tone || (direction === "up" ? "pos" : direction === "down" ? "neg" : undefined);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-kpi", className)
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "lw-kpi-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, label), icon && /*#__PURE__*/React.createElement("span", {
    className: "lw-kpi-badge",
    "data-accent": accent
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 18
  }))), /*#__PURE__*/React.createElement("span", {
    className: "lw-kpi-row"
  }, /*#__PURE__*/React.createElement("span", {
    className: "v"
  }, value), (delta || note) && /*#__PURE__*/React.createElement("span", {
    className: cx("d", direction, ink)
  }, direction && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: direction === "up" ? "arrow-up" : "arrow-down",
    size: 13
  }), delta, note && /*#__PURE__*/React.createElement("span", {
    className: "w"
  }, note))));
}
Object.assign(__ds_scope, { KpiTile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/KpiTile.jsx", error: String((e && e.message) || e) }); }

// components/data/Pagination.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* First, last, the current page and one neighbour either side — with gaps for
   everything else. A strip that renders every page is unusable at 200 pages and
   a strip that renders only prev/next hides where you are. */
function pages(page, count) {
  const out = [];
  const push = p => {
    if (out[out.length - 1] !== p) out.push(p);
  };
  for (let p = 1; p <= count; p++) {
    if (p === 1 || p === count || Math.abs(p - page) <= 1) push(p);else if (out[out.length - 1] !== "gap") out.push("gap");
  }
  return out;
}

/**
 * Page navigation and the result count — one component, because the count is
 * what tells a user whether their filter did anything, and a strip of numbers
 * without it is a control with no feedback.
 *
 * `total` + `pageSize` drives the numbered mode. Pass `cursor` instead for an
 * API that cannot count: prev/next only, no page numbers, no total, because
 * inventing either would be a lie.
 */
function Pagination({
  page = 1,
  pageSize = 25,
  total,
  onPageChange,
  onPageSizeChange,
  pageSizes = [25, 50, 100],
  cursor,
  hasNext,
  hasPrev,
  label = "Pagination",
  className,
  ...rest
}) {
  const count = total != null ? Math.max(1, Math.ceil(total / pageSize)) : 1;
  const from = total ? (page - 1) * pageSize + 1 : 0;
  const to = total ? Math.min(page * pageSize, total) : 0;
  const nf = new Intl.NumberFormat();
  const go = p => onPageChange && onPageChange(Math.min(Math.max(1, p), count));
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cx("lw-pagination", className),
    "aria-label": label
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "lw-pag-info"
  }, cursor ? "Page " + page : total ? nf.format(from) + "–" + nf.format(to) + " of " + nf.format(total) : ""), /*#__PURE__*/React.createElement("span", {
    className: "lw-pag-spacer"
  }), onPageSizeChange && !cursor && /*#__PURE__*/React.createElement("select", {
    className: "lw-input lw-input-sm",
    style: {
      width: "auto"
    },
    "aria-label": "Rows per page",
    value: pageSize,
    onChange: e => onPageSizeChange(Number(e.target.value))
  }, pageSizes.map(s => /*#__PURE__*/React.createElement("option", {
    key: s,
    value: s
  }, s, " / page"))), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-pag-btn",
    "aria-label": "Previous page",
    disabled: cursor ? !hasPrev : page <= 1,
    onClick: () => go(page - 1)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-left",
    size: 15
  })), !cursor && pages(page, count).map((p, i) => p === "gap" ? /*#__PURE__*/React.createElement("span", {
    key: "g" + i,
    className: "lw-pag-gap",
    "aria-hidden": "true"
  }, "\u2026") : /*#__PURE__*/React.createElement("button", {
    key: p,
    type: "button",
    className: "lw-pag-btn",
    "aria-label": "Page " + p,
    "aria-current": p === page ? "page" : undefined,
    onClick: () => go(p)
  }, p)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-pag-btn",
    "aria-label": "Next page",
    disabled: cursor ? !hasNext : page >= count,
    onClick: () => go(page + 1)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-right",
    size: 15
  })));
}
Object.assign(__ds_scope, { Pagination });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Pagination.jsx", error: String((e && e.message) || e) }); }

// components/data/StatMeter.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * A number that reads as a measurement rather than a decoration: a unit, a
 * direction, a bar, and — where one exists — a target marker. Without the
 * target a reader cannot tell whether 94% is good news.
 */
function StatMeter({
  label,
  value,
  unit,
  delta,
  direction,
  percent,
  target,
  tone,
  foot,
  interactive = false,
  className,
  ...rest
}) {
  // A native <button> cannot hold the bar and the stat row — its content model
  // is phrasing content, and this tile is divs. So the interactive form takes
  // the full ARIA button contract instead of a bare tabindex: a role, and
  // Enter/Space activation. A focusable element with no role and no key handler
  // is a tab stop that does nothing.
  const onKeyDown = e => {
    if (!interactive || e.key !== "Enter" && e.key !== " ") return;
    e.preventDefault();
    rest.onClick && rest.onClick(e);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-card", "lw-stat-tile", interactive && "lw-card-interactive", className),
    role: interactive ? "button" : undefined,
    tabIndex: interactive ? 0 : undefined,
    onKeyDown: interactive ? onKeyDown : undefined
  }, rest), label && /*#__PURE__*/React.createElement("span", {
    className: "lw-card-eyebrow"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "lw-stat-row"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lw-stat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "n",
    style: {
      fontSize: 32
    }
  }, value, unit && /*#__PURE__*/React.createElement("span", {
    className: "u"
  }, unit))), delta && /*#__PURE__*/React.createElement("span", {
    className: "lw-stat-delta",
    "data-dir": direction
  }, (direction === "up" || direction === "down") && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "arrow-" + direction,
    size: 13
  }), delta)), percent != null && /*#__PURE__*/React.createElement("div", {
    className: "lw-bar",
    "data-tone": tone,
    style: {
      "--lw-bar-value": percent + "%"
    },
    role: "meter",
    "aria-valuenow": percent,
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    "aria-label": typeof label === "string" ? label : undefined,
    "aria-valuetext": target != null ? percent + "% of a " + target + "% target" : undefined
  }, /*#__PURE__*/React.createElement("i", {
    className: "fill"
  }), target != null && /*#__PURE__*/React.createElement("span", {
    className: "target",
    style: {
      left: target + "%"
    }
  })), foot && /*#__PURE__*/React.createElement("span", {
    className: "lw-stat-foot"
  }, foot));
}
Object.assign(__ds_scope, { StatMeter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StatMeter.jsx", error: String((e && e.message) || e) }); }

// components/data/Table.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Hairline rows, mono tabular numerics, a sticky head. Pass `columns` +
 * `rows` for the common case; children for anything else.
 *
 * A column with `num: true` gets tabular-nums and right alignment — which is
 * the whole reason a table reads as data rather than as text.
 *
 * Sorting: give a column `sortable` and pass `onSort(key, nextDirection)`. The
 * header then renders as a real button with a direction caret, because
 * `aria-sort` on a th nothing can focus is a promise the keyboard cannot keep.
 */
function Table({
  columns,
  rows,
  hover = true,
  compact = false,
  caption,
  onSort,
  className,
  children,
  ...rest
}) {
  const head = columns && /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map(c => {
    const sortable = (c.sortable || c.sort) && onSort;
    const sort = c.sort === "asc" ? "ascending" : c.sort === "desc" ? "descending" : c.sort;
    return /*#__PURE__*/React.createElement("th", {
      key: c.key,
      className: c.num ? "num" : undefined,
      scope: "col",
      "aria-sort": sortable ? sort || "none" : sort || undefined
    }, sortable ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: () => onSort(c.key, sort === "ascending" ? "desc" : "asc")
    }, c.label, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: sort === "descending" ? "chevron-down" : "chevron-up",
      size: 12
    })) : c.label);
  })));
  return /*#__PURE__*/React.createElement("div", {
    className: "lw-table-wrap lw-scroll"
  }, /*#__PURE__*/React.createElement("table", _extends({
    className: cx("lw-table", hover && "lw-table-hover", compact && "lw-table-compact", className)
  }, rest), caption && /*#__PURE__*/React.createElement("caption", {
    className: "lw-sr-only"
  }, caption), head, rows ? /*#__PURE__*/React.createElement("tbody", null, rows.map((r, i) => /*#__PURE__*/React.createElement("tr", {
    key: r.id ?? i
  }, columns.map(c => /*#__PURE__*/React.createElement("td", {
    key: c.key,
    className: cx(c.num && "num", c.muted && "muted")
  }, r[c.key]))))) : children));
}
Object.assign(__ds_scope, { Table });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Table.jsx", error: String((e && e.message) || e) }); }

// components/forms/Calendar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const day = d => new Date(d.getFullYear(), d.getMonth(), d.getDate());
const same = (a, b) => a && b && day(a).getTime() === day(b).getTime();
const addDays = (d, n) => {
  const x = day(d);
  x.setDate(x.getDate() + n);
  return x;
};
const addMonths = (d, n) => {
  const x = day(d);
  x.setDate(1);
  x.setMonth(x.getMonth() + n);
  return x;
};
const between = (d, a, b) => a && b && day(d) > day(a) && day(d) < day(b);

/**
 * The date grid. Real <button> cells with a roving tabindex — a date grid is only
 * usable by keyboard if each cell can hold focus, and Tab should enter and leave
 * the grid once rather than walk 42 days.
 *
 * Month names and weekday initials come from Intl, so the calendar is localised
 * by the browser rather than by a hardcoded English array.
 */
function Calendar({
  value,
  onChange,
  range,
  month,
  onMonthChange,
  min,
  max,
  weekStart = 1,
  locale,
  className,
  ...rest
}) {
  const sel = range ? value || {} : value;
  const anchor = (range ? sel.start : sel) || new Date();
  const [viewRaw, setView] = React.useState(() => addMonths(anchor, 0));
  const view = month || viewRaw;
  const setMonth = m => {
    onMonthChange ? onMonthChange(m) : setView(m);
  };
  const [focused, setFocused] = React.useState(() => day(anchor));
  // A hover preview is what makes a range feel like a range before the second
  // click — without it the first click looks like it did nothing.
  const [hover, setHover] = React.useState(null);
  const gridRef = React.useRef(null);
  // Set only by keyboard navigation, so a re-render from a click or a hover never
  // yanks focus back into the grid.
  const navving = React.useRef(false);
  const fmtMonth = new Intl.DateTimeFormat(locale, {
    month: "long",
    year: "numeric"
  });
  const fmtDow = new Intl.DateTimeFormat(locale, {
    weekday: "narrow"
  });
  const fmtFull = new Intl.DateTimeFormat(locale, {
    weekday: "long",
    day: "numeric",
    month: "long",
    year: "numeric"
  });
  const first = new Date(view.getFullYear(), view.getMonth(), 1);
  const lead = (first.getDay() - weekStart + 7) % 7;
  const cells = Array.from({
    length: 42
  }, (_, i) => addDays(first, i - lead));
  const dows = Array.from({
    length: 7
  }, (_, i) => fmtDow.format(addDays(new Date(2024, 0, 7 + weekStart), i)));
  const disabled = d => min && day(d) < day(min) || max && day(d) > day(max);
  const isSelected = d => range ? same(d, sel.start) || same(d, sel.end) : same(d, sel);
  const inRange = d => {
    if (!range) return false;
    const end = sel.end || sel.start && hover;
    return between(d, sel.start, end) || between(d, end, sel.start);
  };
  const pick = d => {
    if (disabled(d)) return;
    if (!range) return onChange && onChange(day(d));
    // Second click BEFORE the first is a correction, not an error: swap rather
    // than reject, because the user is telling you the range they meant.
    if (!sel.start || sel.end) return onChange && onChange({
      start: day(d),
      end: null
    });
    const [s, e] = day(d) < day(sel.start) ? [day(d), sel.start] : [sel.start, day(d)];
    onChange && onChange({
      start: s,
      end: e
    });
  };
  const moveFocus = next => {
    navving.current = true;
    setFocused(next);
    if (next.getMonth() !== view.getMonth()) setMonth(addMonths(next, 0));
  };
  // An effect, not requestAnimationFrame: rAF does not run in a hidden or
  // throttled document, so the focus would silently never land — and the cell
  // that should take it does not exist until this render has committed.
  React.useEffect(() => {
    if (!navving.current) return;
    navving.current = false;
    const el = gridRef.current && gridRef.current.querySelector('[tabindex="0"]');
    if (el) el.focus({
      preventScroll: true
    });
  }, [focused]);
  const onKeyDown = e => {
    const k = e.key;
    const d = {
      ArrowRight: 1,
      ArrowLeft: -1,
      ArrowDown: 7,
      ArrowUp: -7
    }[k];
    if (d) {
      e.preventDefault();
      return moveFocus(addDays(focused, d));
    }
    if (k === "PageUp") {
      e.preventDefault();
      return moveFocus(addMonths(focused, -1));
    }
    if (k === "PageDown") {
      e.preventDefault();
      return moveFocus(addMonths(focused, 1));
    }
    if (k === "Home") {
      e.preventDefault();
      return moveFocus(addDays(focused, -((focused.getDay() - weekStart + 7) % 7)));
    }
    if (k === "End") {
      e.preventDefault();
      return moveFocus(addDays(focused, 6 - (focused.getDay() - weekStart + 7) % 7));
    }
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-cal", className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-cal-head"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Previous month",
    onClick: () => setMonth(addMonths(view, -1))
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-left",
    size: 16
  })), /*#__PURE__*/React.createElement("div", {
    className: "lw-cal-month",
    "aria-live": "polite"
  }, fmtMonth.format(view)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Next month",
    onClick: () => setMonth(addMonths(view, 1))
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-right",
    size: 16
  }))), /*#__PURE__*/React.createElement("div", {
    className: "lw-cal-grid",
    "aria-hidden": "true"
  }, dows.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "lw-cal-dow"
  }, d))), /*#__PURE__*/React.createElement("div", {
    ref: gridRef,
    className: "lw-cal-grid",
    role: "grid",
    onKeyDown: onKeyDown,
    onMouseLeave: () => setHover(null)
  }, cells.map((d, i) => {
    const outside = d.getMonth() !== view.getMonth();
    const selected = isSelected(d);
    const end = range ? sel.end || hover : null;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "gridcell",
      className: "lw-cal-day",
      tabIndex: same(d, focused) ? 0 : -1,
      "aria-selected": selected,
      "aria-label": fmtFull.format(d),
      disabled: disabled(d),
      "data-outside": outside ? "true" : undefined,
      "data-today": same(d, new Date()) ? "true" : undefined,
      "data-in-range": inRange(d) ? "true" : undefined,
      "data-edge": range && selected ? same(d, sel.start) && end ? "start" : same(d, sel.end) ? "end" : undefined : undefined,
      onMouseEnter: () => range && sel.start && !sel.end && setHover(day(d)),
      onFocus: () => setFocused(day(d)),
      onClick: () => pick(d)
    }, d.getDate());
  })));
}
Object.assign(__ds_scope, { Calendar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Calendar.jsx", error: String((e && e.message) || e) }); }

// components/forms/Combobox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const norm = o => typeof o === "string" || typeof o === "number" ? {
  value: o,
  label: String(o)
} : o;

/**
 * The single- and multi-select combobox — the control that gets rebuilt in every
 * product because a native <select> cannot filter and a listbox is 200 lines of
 * ARIA. Built on Popover, so it shares one floating surface with Menu.
 *
 * Focus stays in the INPUT and the active option is named by
 * `aria-activedescendant`. That is the ARIA 1.2 combobox pattern: moving real
 * focus into the list would take it out of the field the user is still typing in.
 * (Menu is the opposite — there is nothing to type into, so focus moves.)
 */
function Combobox({
  options = [],
  value,
  onChange,
  multiple,
  placeholder,
  size = "md",
  invalid,
  disabled,
  loading,
  emptyText = "No matches",
  onSearch,
  id,
  label,
  className,
  ...rest
}) {
  const opts = React.useMemo(() => options.map(norm), [options]);
  const [open, setOpen] = React.useState(false);
  const [query, setQuery] = React.useState("");
  const [active, setActive] = React.useState(0);
  const inputRef = React.useRef(null);
  const listRef = React.useRef(null);
  const uid = React.useId();
  const listId = uid + "-list";
  const inputId = id || uid + "-in";
  const selected = multiple ? Array.isArray(value) ? value : [] : value;
  const selectedOpts = multiple ? opts.filter(o => selected.includes(o.value)) : [];
  const current = !multiple ? opts.find(o => o.value === value) : null;

  // When the caller owns search (onSearch), the options ARE the result — filtering
  // them again locally would hide rows the server deliberately returned.
  const shown = React.useMemo(() => {
    if (onSearch || !query) return opts;
    const q = query.toLowerCase();
    return opts.filter(o => String(o.label).toLowerCase().includes(q));
  }, [opts, query, onSearch]);
  React.useEffect(() => {
    if (active >= shown.length) setActive(0);
  }, [shown.length, active]);

  // Keyboard scrolling must follow the active option, or arrowing past the tenth
  // row moves a highlight the user cannot see.
  React.useEffect(() => {
    if (!open || !listRef.current) return;
    const el = listRef.current.querySelector('[data-active="true"]');
    if (el && el.offsetParent) {
      const box = listRef.current.parentElement;
      if (el.offsetTop < box.scrollTop) box.scrollTop = el.offsetTop;else if (el.offsetTop + el.offsetHeight > box.scrollTop + box.clientHeight) box.scrollTop = el.offsetTop + el.offsetHeight - box.clientHeight;
    }
  }, [active, open]);
  const commit = o => {
    if (!o || o.disabled) return;
    if (multiple) {
      const next = selected.includes(o.value) ? selected.filter(v => v !== o.value) : [...selected, o.value];
      onChange && onChange(next);
      setQuery("");
      // Stay open: picking one of several is rarely picking the last one.
    } else {
      onChange && onChange(o.value);
      setQuery("");
      setOpen(false);
    }
    inputRef.current && inputRef.current.focus({
      preventScroll: true
    });
  };
  const remove = v => onChange && onChange(selected.filter(x => x !== v));
  const onKeyDown = e => {
    if (e.key === "ArrowDown" || e.key === "ArrowUp") {
      e.preventDefault();
      if (!open) {
        setOpen(true);
        return;
      }
      const d = e.key === "ArrowDown" ? 1 : -1;
      setActive(i => (i + d + shown.length) % Math.max(shown.length, 1));
      return;
    }
    if (e.key === "Enter" && open) {
      e.preventDefault();
      return commit(shown[active]);
    }
    if (e.key === "Escape" && open) {
      e.preventDefault();
      return setOpen(false);
    }
    // Backspace is the SHORTCUT for removing the last token; the token's own
    // button is the affordance. Only fires on an empty query, so it can never eat
    // a token the user was still typing past.
    if (e.key === "Backspace" && multiple && !query && selected.length) return remove(selected[selected.length - 1]);
  };
  const field = /*#__PURE__*/React.createElement("div", {
    className: cx("lw-combo", size === "sm" && "lw-combo-sm", size === "lg" && "lw-combo-lg", className),
    "data-disabled": disabled ? "true" : undefined,
    onMouseDown: e => {
      if (e.target === e.currentTarget && inputRef.current) inputRef.current.focus();
    }
  }, selectedOpts.map(o => /*#__PURE__*/React.createElement("span", {
    key: o.value,
    className: "lw-combo-token"
  }, /*#__PURE__*/React.createElement("span", null, o.label), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Remove " + o.label,
    onMouseDown: e => e.preventDefault(),
    onClick: () => remove(o.value)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 11
  })))), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    id: inputId,
    role: "combobox",
    type: "text",
    autoComplete: "off",
    "aria-expanded": open,
    "aria-controls": listId,
    "aria-autocomplete": "list",
    "aria-label": label,
    "aria-activedescendant": open && shown[active] ? listId + "-" + active : undefined,
    "aria-invalid": invalid ? "true" : undefined,
    disabled: disabled,
    placeholder: current ? undefined : multiple && selectedOpts.length ? "" : placeholder,
    value: query || (!multiple && !open && current ? current.label : query),
    onChange: e => {
      setQuery(e.target.value);
      setActive(0);
      setOpen(true);
      onSearch && onSearch(e.target.value);
    },
    onKeyDown: onKeyDown,
    onFocus: () => setOpen(true)
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-combo-chev"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevrons-up-down",
    size: 15
  })));
  return /*#__PURE__*/React.createElement(__ds_scope.Popover, _extends({
    trigger: field,
    open: open && !disabled,
    onOpenChange: setOpen,
    role: "listbox",
    triggerAria: false,
    matchWidth: true,
    placement: "bottom-start",
    label: label
  }, rest), loading ? /*#__PURE__*/React.createElement("div", {
    className: "lw-listbox-empty"
  }, "Searching\u2026") : !shown.length ? /*#__PURE__*/React.createElement("div", {
    className: "lw-listbox-empty"
  }, emptyText) : /*#__PURE__*/React.createElement("ul", {
    ref: listRef,
    className: "lw-listbox",
    id: listId,
    role: "listbox",
    "aria-multiselectable": multiple || undefined
  }, shown.map((o, i) => {
    const isSel = multiple ? selected.includes(o.value) : o.value === value;
    return /*#__PURE__*/React.createElement("li", {
      key: o.value,
      id: listId + "-" + i,
      className: "lw-option",
      role: "option",
      "aria-selected": isSel,
      "aria-disabled": o.disabled ? "true" : undefined,
      "data-active": i === active ? "true" : undefined,
      onMouseEnter: () => setActive(i),
      onMouseDown: e => e.preventDefault(),
      onClick: () => commit(o)
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-option-lead"
    }, isSel && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "checkmark",
      size: 14
    })), /*#__PURE__*/React.createElement("span", {
      className: "lw-option-text"
    }, o.label), o.meta && /*#__PURE__*/React.createElement("span", {
      className: "lw-option-meta"
    }, o.meta));
  })));
}
Object.assign(__ds_scope, { Combobox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Combobox.jsx", error: String((e && e.message) || e) }); }

// components/forms/DatePicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const day = d => new Date(d.getFullYear(), d.getMonth(), d.getDate());
const shift = n => {
  const d = new Date();
  d.setDate(d.getDate() + n);
  return day(d);
};

/** The presets are the point of a range picker. "Last 7 days" is what a user
 *  actually wants nine times out of ten, and making them build it from two
 *  clicks on a grid is the difference between a control and a chore. */
const RANGE_PRESETS = [{
  label: "Today",
  get: () => ({
    start: day(new Date()),
    end: day(new Date())
  })
}, {
  label: "Last 7 days",
  get: () => ({
    start: shift(-6),
    end: day(new Date())
  })
}, {
  label: "Last 30 days",
  get: () => ({
    start: shift(-29),
    end: day(new Date())
  })
}, {
  label: "Last 90 days",
  get: () => ({
    start: shift(-89),
    end: day(new Date())
  })
}];

/**
 * The date field. `range` turns it into a range picker with presets down the
 * left — on the same `Popover` as Menu and Combobox, so there is still one
 * floating surface in the system.
 */
function DatePicker({
  value,
  onChange,
  range,
  presets = RANGE_PRESETS,
  min,
  max,
  size = "md",
  invalid,
  disabled,
  placeholder,
  locale,
  label,
  id,
  className,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  const uid = React.useId();
  const fmt = new Intl.DateTimeFormat(locale, {
    day: "numeric",
    month: "short",
    year: "numeric"
  });
  const text = React.useMemo(() => {
    if (range) {
      const v = value || {};
      if (!v.start) return "";
      return fmt.format(v.start) + (v.end ? " – " + fmt.format(v.end) : " – …");
    }
    return value ? fmt.format(value) : "";
  }, [value, range, locale]);
  const activePreset = range && value && value.start && value.end ? presets.findIndex(p => {
    const r = p.get();
    return r.start.getTime() === day(value.start).getTime() && r.end.getTime() === day(value.end).getTime();
  }) : -1;
  const field = /*#__PURE__*/React.createElement("button", {
    type: "button",
    id: id || uid,
    disabled: disabled,
    "aria-invalid": invalid ? "true" : undefined,
    "aria-label": label,
    className: cx("lw-input", size === "sm" && "lw-input-sm", size === "lg" && "lw-input-lg", className),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--lw-space-8)",
      textAlign: "left",
      cursor: disabled ? "not-allowed" : "pointer"
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "calendar",
    size: 15,
    style: {
      flex: "none",
      color: "var(--lw-fg-subtle)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap",
      color: text ? "var(--lw-fg)" : "var(--lw-fg-subtle)"
    }
  }, text || placeholder || (range ? "Pick a range" : "Pick a date")));
  return /*#__PURE__*/React.createElement(__ds_scope.Popover, _extends({
    trigger: field,
    open: open && !disabled,
    onOpenChange: setOpen,
    padded: true,
    role: "dialog",
    label: label || (range ? "Choose a date range" : "Choose a date"),
    placement: "bottom-start"
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-cal-wrap"
  }, range && presets.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "lw-cal-presets"
  }, presets.map((p, i) => /*#__PURE__*/React.createElement("button", {
    key: p.label,
    type: "button",
    className: "lw-cal-preset",
    "aria-pressed": i === activePreset,
    onClick: () => {
      onChange && onChange(p.get());
      setOpen(false);
    }
  }, p.label))), /*#__PURE__*/React.createElement(__ds_scope.Calendar, {
    value: value,
    onChange: v => {
      onChange && onChange(v);
      // A single date is complete on one click; a range is not complete
      // until it has an end, so the panel stays open for the second one.
      if (!range) setOpen(false);else if (v && v.end) setOpen(false);
    },
    range: range,
    min: min,
    max: max,
    locale: locale
  })));
}
Object.assign(__ds_scope, { RANGE_PRESETS, DatePicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/DatePicker.jsx", error: String((e && e.message) || e) }); }

// components/forms/FileUpload.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const KB = 1024;
function formatBytes(n) {
  if (n == null) return "";
  const u = ["B", "KB", "MB", "GB"];
  let i = 0,
    v = n;
  while (v >= KB && i < u.length - 1) {
    v /= KB;
    i++;
  }
  return (i === 0 ? v : v.toFixed(v < 10 ? 1 : 0)) + " " + u[i];
}

/**
 * The dropzone is a LABEL wrapping a real file input, so the click target, the
 * keyboard path and the accessible name are the platform's rather than a div
 * with a handler on it. Drag is the enhancement; click and Enter are the control.
 *
 * The drag counter is not decoration: dragleave fires when the pointer crosses
 * onto a CHILD element, so a boolean flag flickers off every time the cursor
 * passes over the icon inside the zone.
 */
function FileUpload({
  files = [],
  onFiles,
  onRemove,
  accept,
  multiple,
  maxSize,
  disabled,
  title = "Drop files here",
  hint,
  className,
  ...rest
}) {
  const [over, setOver] = React.useState(0);
  const [rejected, setRejected] = React.useState(null);
  const inputRef = React.useRef(null);
  const take = list => {
    const arr = Array.from(list || []);
    if (!arr.length) return;
    const tooBig = maxSize ? arr.filter(f => f.size > maxSize) : [];
    // Say WHICH file and WHY, with the limit — "upload failed" is not a message.
    setRejected(tooBig.length ? tooBig.map(f => f.name).join(", ") + " — over " + formatBytes(maxSize) : null);
    const ok = maxSize ? arr.filter(f => f.size <= maxSize) : arr;
    if (ok.length && onFiles) onFiles(multiple ? ok : ok.slice(0, 1));
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx(className)
  }, rest), /*#__PURE__*/React.createElement("label", {
    className: "lw-dropzone",
    "data-over": over > 0 ? "true" : undefined,
    "data-disabled": disabled ? "true" : undefined,
    onDragEnter: e => {
      e.preventDefault();
      if (!disabled) setOver(o => o + 1);
    },
    onDragOver: e => e.preventDefault(),
    onDragLeave: () => setOver(o => Math.max(0, o - 1)),
    onDrop: e => {
      e.preventDefault();
      setOver(0);
      if (!disabled) take(e.dataTransfer.files);
    }
  }, /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    type: "file",
    accept: accept,
    multiple: multiple,
    disabled: disabled,
    onChange: e => {
      take(e.target.files);
      e.target.value = "";
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "upload",
    size: 20
  }), /*#__PURE__*/React.createElement("span", {
    className: "lw-dz-title"
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "lw-dz-hint"
  }, hint || (accept ? accept + (maxSize ? " · up to " + formatBytes(maxSize) : "") : maxSize ? "Up to " + formatBytes(maxSize) : "or click to browse"))), rejected && /*#__PURE__*/React.createElement("div", {
    className: "lw-error",
    role: "alert",
    style: {
      marginTop: "var(--lw-space-8)"
    }
  }, rejected), files.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "lw-file-list"
  }, files.map((f, i) => /*#__PURE__*/React.createElement("div", {
    key: f.id ?? f.name + i,
    className: "lw-file-row",
    "data-state": f.state,
    style: f.progress != null ? {
      "--lw-file-pct": f.progress + "%"
    } : undefined
  }, /*#__PURE__*/React.createElement("span", {
    className: "lw-file-ic"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: f.state === "error" ? "x-circle" : f.state === "done" ? "check" : "file",
    size: 16
  })), /*#__PURE__*/React.createElement("span", {
    className: "lw-file-main"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lw-file-name"
  }, f.name), f.state === "uploading" && f.progress != null ? /*#__PURE__*/React.createElement("span", {
    className: "lw-file-bar"
  }, /*#__PURE__*/React.createElement("i", null)) : /*#__PURE__*/React.createElement("span", {
    className: "lw-file-meta"
  }, f.error || formatBytes(f.size))), onRemove && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Remove " + f.name,
    onClick: () => onRemove(f)
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 15
  }))))));
}
Object.assign(__ds_scope, { formatBytes, FileUpload });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/FileUpload.jsx", error: String((e && e.message) || e) }); }

// components/forms/RichText.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* The toolbar is DATA, so swapping the engine underneath does not touch it — and
   so a product can drop a control by filtering an array rather than editing JSX. */
const TOOLS = [{
  id: "bold",
  icon: "spark",
  label: "Bold",
  cmd: "bold",
  glyph: "B"
}, {
  id: "italic",
  icon: "spark",
  label: "Italic",
  cmd: "italic",
  glyph: "I"
}, {
  sep: true
}, {
  id: "h2",
  icon: "list",
  label: "Heading",
  cmd: "formatBlock",
  arg: "h2",
  glyph: "H"
}, {
  id: "ul",
  icon: "list",
  label: "Bulleted list",
  cmd: "insertUnorderedList"
}, {
  id: "ol",
  icon: "sort-asc",
  label: "Numbered list",
  cmd: "insertOrderedList"
}, {
  id: "quote",
  icon: "quote",
  label: "Quote",
  cmd: "formatBlock",
  arg: "blockquote"
}, {
  id: "code",
  icon: "code",
  label: "Code",
  cmd: "formatBlock",
  arg: "pre"
}, {
  sep: true
}, {
  id: "link",
  icon: "link",
  label: "Link",
  cmd: "createLink",
  prompt: "Link URL"
}, {
  id: "clear",
  icon: "undo",
  label: "Clear formatting",
  cmd: "removeFormat"
}];

/**
 * The editor CHROME — a toolbar and a prose surface on the type scale. The
 * editing engine is deliberately not the system's: swap TipTap or ProseMirror in
 * underneath and the toolbar does not change, which is the only way a rich-text
 * editor stops being a fork of the design system.
 *
 * The default engine is the platform's `contenteditable`, which is honest about
 * what it is: fine for a comment box, not for a document. `renderToolbar` and
 * `children` let you keep this chrome and bring your own surface.
 */
function RichText({
  value,
  onChange,
  placeholder = "Write something…",
  tools,
  maxLength,
  label,
  readOnly,
  footer,
  children,
  className,
  ...rest
}) {
  const _lwRtBodyId = React.useId();
  const ref = React.useRef(null);
  const [active, setActive] = React.useState({});
  const list = tools ? TOOLS.filter(t => t.sep || tools.includes(t.id)) : TOOLS;

  // The DOM owns the text while the user types; writing value back on every
  // keystroke would move the caret to the end on each one.
  React.useEffect(() => {
    const el = ref.current;
    if (el && value != null && el.innerHTML !== value) el.innerHTML = value;
  }, [value]);
  const syncActive = () => {
    if (typeof document.queryCommandState !== "function") return;
    const next = {};
    for (const t of list) {
      if (t.sep || !t.cmd) continue;
      try {
        next[t.id] = t.arg ? false : document.queryCommandState(t.cmd);
      } catch (e) {}
    }
    setActive(next);
  };
  const run = t => {
    const el = ref.current;
    if (!el || readOnly) return;
    el.focus();
    let arg = t.arg;
    if (t.prompt) {
      arg = window.prompt(t.prompt);
      if (!arg) return;
    }
    // execCommand is deprecated and this is the shim, not the plan — it is here so
    // the chrome is demonstrable without pulling an engine into the design system.
    try {
      document.execCommand(t.cmd, false, arg);
    } catch (e) {}
    syncActive();
    onChange && onChange(el.innerHTML);
  };
  const text = ref.current && ref.current.textContent || "";
  const over = maxLength != null && text.length > maxLength;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-editor", className)
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-editor-bar",
    role: "toolbar",
    "aria-label": (label || "Editor") + " formatting",
    "aria-controls": _lwRtBodyId
  }, list.map((t, i) => t.sep ? /*#__PURE__*/React.createElement("span", {
    key: "s" + i,
    className: "sep",
    "aria-hidden": "true"
  }) : /*#__PURE__*/React.createElement("button", {
    key: t.id,
    type: "button",
    className: "lw-icon-btn",
    "aria-label": t.label,
    title: t.label,
    "aria-pressed": !!active[t.id],
    disabled: readOnly,
    onMouseDown: e => e.preventDefault(),
    onClick: () => run(t)
  }, t.glyph ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: t.id === "bold" ? 700 : 500,
      fontStyle: t.id === "italic" ? "italic" : undefined,
      fontSize: 13
    }
  }, t.glyph) : /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: t.icon,
    size: 15
  })))), children || /*#__PURE__*/React.createElement("div", {
    ref: ref,
    id: _lwRtBodyId,
    className: "lw-editor-body",
    contentEditable: !readOnly,
    suppressContentEditableWarning: true,
    role: "textbox",
    "aria-multiline": "true",
    "aria-label": label,
    "data-placeholder": placeholder,
    onInput: () => onChange && onChange(ref.current.innerHTML),
    onKeyUp: syncActive,
    onMouseUp: syncActive
  }), (footer || maxLength != null) && /*#__PURE__*/React.createElement("div", {
    className: "lw-editor-foot"
  }, footer, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), maxLength != null && /*#__PURE__*/React.createElement("span", {
    style: {
      color: over ? "var(--lw-danger-on)" : undefined
    },
    "aria-live": "polite"
  }, text.length, " / ", maxLength)));
}
Object.assign(__ds_scope, { RichText });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/RichText.jsx", error: String((e && e.message) || e) }); }

// components/forms/Stepper.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * Wizard progress. The marker carries the state — the number becomes a check
 * once the step is done — so progress survives greyscale and does not depend on
 * the connector's tint (README rule 6).
 *
 * A step is only a BUTTON when it is reachable. Rendering every step as a
 * control and disabling the future ones invites a user to try; rendering them
 * as text says what is true, which is that you get there by finishing this one.
 */
function Stepper({
  steps = [],
  current = 0,
  onStepChange,
  vertical,
  label = "Progress",
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-stepper", vertical && "lw-stepper-vertical", className),
    role: "group",
    "aria-label": label
  }, rest), steps.map((s, i) => {
    const state = s.state || (i < current ? "done" : i === current ? "current" : "upcoming");
    const reachable = onStepChange && (state === "done" || state === "error");
    const Tag = reachable ? "button" : "div";
    return /*#__PURE__*/React.createElement(Tag, {
      key: s.key ?? i,
      className: "lw-stepper-step",
      "data-state": state,
      type: reachable ? "button" : undefined,
      "aria-current": state === "current" ? "step" : undefined,
      onClick: reachable ? () => onStepChange(i) : undefined
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-stepper-marker",
      "aria-hidden": "true"
    }, state === "done" ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "checkmark",
      size: 14
    }) : state === "error" ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "close",
      size: 14
    }) : i + 1), /*#__PURE__*/React.createElement("span", {
      className: "lw-stepper-label"
    }, s.label, /*#__PURE__*/React.createElement("span", {
      className: "lw-sr-only"
    }, " — " + (state === "done" ? "completed" : state === "current" ? "current step" : state === "error" ? "needs attention" : "not started"))), s.hint && /*#__PURE__*/React.createElement("span", {
      className: "lw-stepper-hint"
    }, s.hint));
  }));
}
Object.assign(__ds_scope, { Stepper });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Stepper.jsx", error: String((e && e.message) || e) }); }

// components/marketing/FeatureGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Numbered features on a hairline grid. The brand edge draws in on hover —
 *  a measured rule, not a glow. */
function FeatureGrid({
  features = [],
  className,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-features", className)
  }, rest), features.map((f, i) => {
    const Tag = f.href ? "a" : "div";
    return /*#__PURE__*/React.createElement(Tag, {
      key: i,
      className: cx("lw-feature", f.href && "lw-feature-interactive"),
      href: f.href
    }, /*#__PURE__*/React.createElement("span", {
      className: "num"
    }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("h3", null, f.title), /*#__PURE__*/React.createElement("p", null, f.body), f.href && /*#__PURE__*/React.createElement("span", {
      className: "lw-feature-more"
    }, f.more || "Learn more", /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "arrow-right",
      size: 14,
      className: "arrow"
    })));
  }));
}
Object.assign(__ds_scope, { FeatureGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/marketing/FeatureGrid.jsx", error: String((e && e.message) || e) }); }

// components/nav/AppBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The app chrome as products actually use it: brand, breadcrumbs, an optional
 * sidebar toggle, and actions on the right. Built on `TopBar`, which owns the
 * sticky hairline bar itself.
 *
 * It exists because five templates hand-wrote this row and two of them got the
 * flex wrong — the holder claimed `flex: 1` against TopBar's own spacer, so it
 * settled narrower than its content and the breadcrumbs ellipsised with a third
 * of the row still empty. One component, one correct holder (README rule 9).
 */
function AppBar({
  brand = "LeanWise AI",
  brandHref = "#",
  mark = true,
  crumbs = [],
  onMenuClick,
  menuExpanded,
  actions,
  className,
  children,
  ...rest
}) {
  const Brand = brandHref ? "a" : "span";
  return /*#__PURE__*/React.createElement(__ds_scope.TopBar, _extends({
    className: className
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-appbar-lead"
  }, onMenuClick && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    onClick: onMenuClick,
    "aria-expanded": menuExpanded,
    "aria-label": menuExpanded ? "Collapse navigation" : "Expand navigation"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "sidebar",
    size: 21
  })), /*#__PURE__*/React.createElement(Brand, {
    className: "lw-appbar-brand",
    href: brandHref || undefined,
    "aria-label": brandHref ? typeof brand === "string" ? brand + " — home" : "Home" : undefined
  }, mark && /*#__PURE__*/React.createElement("span", {
    className: "brand-mark",
    "aria-hidden": "true"
  }), brand), crumbs.length > 0 && /*#__PURE__*/React.createElement(__ds_scope.Breadcrumbs, {
    items: crumbs
  })), actions, children);
}
Object.assign(__ds_scope, { AppBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/AppBar.jsx", error: String((e && e.message) || e) }); }

// components/nav/BottomNav.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The touch answer to `Sidebar`. Three to five DESTINATIONS, never actions — a
 * tab bar that mixes "Home" with "New" teaches nobody where they are, and the
 * one thing a tab bar has to communicate is location.
 *
 * It reserves the home indicator with `--lw-safe-bottom` rather than a magic
 * 34px: the inset is 0 on a device without one, so the same rule is right
 * everywhere. The 44px target comes from the bar height, not from padding.
 */
function BottomNav({
  items = [],
  value,
  onChange,
  label = "Main",
  className,
  ...rest
}) {
  if (items.length > 5 && typeof console !== "undefined") {
    console.warn("BottomNav: " + items.length + " items. Past five, labels truncate and the bar stops being scannable — use a sidebar or a More destination.");
  }
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cx("lw-bottom-nav", className),
    "aria-label": label
  }, rest), items.map(it => {
    const on = it.value === value;
    const Tag = it.href ? "a" : "button";
    return /*#__PURE__*/React.createElement(Tag, {
      key: it.value,
      href: it.href || undefined,
      type: it.href ? undefined : "button",
      "aria-current": on ? "page" : undefined,
      onClick: it.href ? undefined : () => onChange && onChange(it.value)
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: it.icon,
      size: 21
    }), /*#__PURE__*/React.createElement("span", {
      className: "lw-bn-label"
    }, it.label), it.badge != null && /*#__PURE__*/React.createElement("span", {
      className: "lw-sr-only"
    }, it.badge + " unread"));
  }));
}
Object.assign(__ds_scope, { BottomNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/BottomNav.jsx", error: String((e && e.message) || e) }); }

// components/nav/CommandPalette.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* Subsequence match, not substring: "opdb" should find "Open database". Scored
   so an early, contiguous match ranks above a scattered one, because a palette
   that returns the right row in ninth place is a palette people stop using. */
function score(query, text) {
  if (!query) return 0;
  const q = query.toLowerCase(),
    s = String(text).toLowerCase();
  let i = 0,
    hit = 0,
    run = 0,
    best = 0;
  for (let n = 0; n < s.length && i < q.length; n++) {
    if (s[n] === q[i]) {
      i++;
      run++;
      hit += run + (n === 0 || s[n - 1] === " " ? 3 : 0);
      best = Math.max(best, run);
    } else run = 0;
  }
  return i === q.length ? hit + best : -1;
}

/**
 * The command palette. A DIALOG, not a popover: it is modal, it takes the whole
 * keyboard, and the page behind it should be inert — so it is the native
 * element, with the platform's focus trap, for the same reason Dialog is.
 *
 * The component does NOT bind ⌘K. A palette that installs a global key handler
 * fights the host app for it and cannot be turned off on the one screen where
 * ⌘K means something else. Bind it where you mount it.
 */
function CommandPalette({
  open,
  onClose,
  commands = [],
  onRun,
  placeholder = "Type a command or search…",
  emptyText = "No matches",
  label = "Command palette",
  className,
  ...rest
}) {
  const ref = React.useRef(null);
  const inputRef = React.useRef(null);
  const [q, setQ] = React.useState("");
  const [active, setActive] = React.useState(0);
  const uid = React.useId();
  React.useEffect(() => {
    const el = ref.current;
    if (!el) return;
    if (open && !el.open) {
      el.showModal();
      setQ("");
      setActive(0);
    }
    if (!open && el.open) el.close();
  }, [open]);

  // The input is focused from an effect rather than autoFocus: autoFocus only
  // fires on mount, and this dialog is mounted long before it is opened.
  React.useEffect(() => {
    if (open && inputRef.current) inputRef.current.focus({
      preventScroll: true
    });
  }, [open]);
  const shown = React.useMemo(() => {
    if (!q) return commands.filter(c => !c.hidden);
    return commands.filter(c => !c.hidden).map(c => ({
      c,
      s: Math.max(score(q, c.label), score(q, c.group || "") - 4, ...(c.keywords || []).map(k => score(q, k) - 2))
    })).filter(x => x.s >= 0).sort((a, b) => b.s - a.s).map(x => x.c);
  }, [q, commands]);
  React.useEffect(() => {
    setActive(0);
  }, [q]);
  const run = c => {
    if (!c || c.disabled) return;
    onClose && onClose();
    c.run ? c.run(c) : onRun && onRun(c);
  };
  const onKeyDown = e => {
    if (e.key === "ArrowDown") {
      e.preventDefault();
      setActive(i => (i + 1) % Math.max(shown.length, 1));
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      setActive(i => (i - 1 + shown.length) % Math.max(shown.length, 1));
    } else if (e.key === "Enter") {
      e.preventDefault();
      run(shown[active]);
    }
  };
  let lastGroup = null;
  return /*#__PURE__*/React.createElement("dialog", _extends({
    ref: ref,
    className: cx("lw-cmdk", className),
    "aria-label": label,
    onClose: onClose,
    onCancel: onClose,
    onKeyDown: onKeyDown
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "lw-cmdk-input"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "search",
    size: 17
  }), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    type: "text",
    role: "combobox",
    "aria-expanded": "true",
    "aria-controls": uid,
    "aria-activedescendant": shown[active] ? uid + "-" + active : undefined,
    "aria-label": label,
    placeholder: placeholder,
    value: q,
    onChange: e => setQ(e.target.value)
  })), /*#__PURE__*/React.createElement("ul", {
    className: "lw-cmdk-list lw-menu",
    id: uid,
    role: "listbox",
    "aria-label": label
  }, !shown.length && /*#__PURE__*/React.createElement("li", {
    className: "lw-listbox-empty"
  }, emptyText), shown.map((c, i) => {
    const head = c.group && c.group !== lastGroup ? lastGroup = c.group : null;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: c.id ?? i
    }, head && /*#__PURE__*/React.createElement("li", {
      className: "lw-menu-label",
      role: "presentation"
    }, head), /*#__PURE__*/React.createElement("li", {
      id: uid + "-" + i,
      role: "option",
      "aria-selected": i === active,
      className: "lw-menu-item",
      "data-active": i === active ? "true" : undefined,
      style: i === active ? {
        background: "var(--lw-bg-subtle)"
      } : undefined,
      onMouseEnter: () => setActive(i),
      onClick: () => run(c)
    }, /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-lead"
    }, c.icon && /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: c.icon,
      size: 15
    })), /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-text"
    }, c.label), c.kbd && /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-kbd"
    }, c.kbd)));
  })), /*#__PURE__*/React.createElement("div", {
    className: "lw-cmdk-foot"
  }, /*#__PURE__*/React.createElement("span", null, "\u2191\u2193 navigate"), /*#__PURE__*/React.createElement("span", null, "\u21B5 run"), /*#__PURE__*/React.createElement("span", null, "esc close")));
}
Object.assign(__ds_scope, { score, CommandPalette });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/CommandPalette.jsx", error: String((e && e.message) || e) }); }

// components/nav/ThemeToggle.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const LABELS = {
  light: "Light",
  dark: "Dark",
  system: "Auto"
};
// Mode -> glyph name. The paths live in Icon, not here: a control that redraws
// an icon it could name is the first place the set drifts out of step.
const GLYPHS = {
  light: "sun",
  dark: "moon",
  system: "monitor"
};

/**
 * Light / dark by default. Every LeanWise surface is authored in both, so two is
 * what the product ships and two is what the control offers — the supported set
 * is declared, never a third segment hidden with CSS.
 *
 * `system` is still a first-class mode: pass `modes={["light","dark","system"]}`
 * wherever a product honours the OS preference. It is worth offering, because a
 * two-state toggle silently overrides `prefers-color-scheme` forever after the
 * first click — it is simply not a promise THIS product keeps on every screen.
 *
 * Uncontrolled by default — reads and writes `document.documentElement` and
 * localStorage. Pass value + onChange to drive it yourself.
 *
 * A page that paints its OWN theme wrapper (a `.dark` / `data-band` container
 * rather than the document root) must pass value + onChange. Left uncontrolled
 * it writes the root while the wrapper keeps its own value, and the page renders
 * half a theme — dark chips on a light ground.
 *
 * Glyphs, not words: the control sits in app chrome where three text labels cost
 * more room than the choice is worth. Each button is named for assistive tech
 * and carries a title, so the meaning survives without the text.
 */
function ThemeToggle({
  value,
  onChange,
  modes = ["light", "dark"],
  className,
  ...rest
}) {
  const [internal, setInternal] = React.useState(modes.includes("system") ? "system" : modes[0]);
  React.useEffect(() => {
    if (value !== undefined) return;
    try {
      const saved = localStorage.getItem("lw-theme");
      if (saved && modes.includes(saved)) setInternal(saved);
    } catch (e) {}
  }, [value]);
  const mode = value !== undefined ? value : internal;
  const apply = m => {
    if (value === undefined) setInternal(m);
    onChange && onChange(m);
    try {
      localStorage.setItem("lw-theme", m);
    } catch (e) {}
    const dark = m === "dark" || m === "system" && window.matchMedia("(prefers-color-scheme: dark)").matches;
    const el = document.documentElement;
    el.classList.toggle("dark", dark);
    el.setAttribute("data-theme", dark ? "dark" : "light");
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-segmented", className),
    role: "group",
    "aria-label": "Colour theme"
  }, rest), modes.map(m => /*#__PURE__*/React.createElement("button", {
    key: m,
    type: "button",
    "aria-pressed": mode === m,
    onClick: () => apply(m),
    "aria-label": LABELS[m] || m,
    title: LABELS[m] || m
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: GLYPHS[m] || "monitor",
    size: 16
  }))));
}
Object.assign(__ds_scope, { ThemeToggle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/nav/ThemeToggle.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Dialog.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The native <dialog>. Focus trap, Esc-to-close and background inertness are
 * the platform's job here rather than 200 lines of ours — and the platform's
 * version is the one screen readers already understand.
 */
function Dialog({
  open,
  onClose,
  title,
  description,
  footer,
  width,
  className,
  children,
  ...rest
}) {
  const ref = React.useRef(null);
  // Ids are generated, not literal. Two dialogs in one document with the same
  // hardcoded id give every one of them the FIRST dialog's title as its name.
  const uid = React.useId();
  const titleId = title ? uid + "-t" : undefined;
  const descId = description ? uid + "-d" : undefined;
  // A bare number OR a numeric string means px. HTML has no numbers — an
  // attribute always arrives as text — and a unitless length makes the width
  // declaration invalid, which silently drops it and shrink-wraps the dialog.
  const w = width == null || width === "" ? null : /^\d+(\.\d+)?$/.test(String(width)) ? String(width) + "px" : String(width);
  React.useEffect(() => {
    const el = ref.current;
    if (!el) return;
    if (open && !el.open) el.showModal();
    if (!open && el.open) el.close();
  }, [open]);
  return /*#__PURE__*/React.createElement("dialog", _extends({
    ref: ref,
    className: cx("lw-dialog", className),
    style: w ? {
      "--lw-dialog-w": w
    } : undefined,
    onClose: onClose,
    onCancel: onClose,
    "aria-labelledby": titleId,
    "aria-describedby": descId
  }, rest), title && /*#__PURE__*/React.createElement("div", {
    className: "lw-dialog-head"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "lw-dialog-title",
    id: titleId
  }, title), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-dialog-close",
    "aria-label": "Close",
    title: "Close",
    onClick: onClose
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 17
  }))), /*#__PURE__*/React.createElement("div", {
    className: "lw-dialog-body"
  }, description && /*#__PURE__*/React.createElement("div", {
    id: descId
  }, description), children), footer && /*#__PURE__*/React.createElement("div", {
    className: "lw-dialog-foot"
  }, footer));
}
Object.assign(__ds_scope, { Dialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Dialog.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Drawer.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/**
 * The side sheet. A drawer is a modal that enters from an edge, not a different
 * kind of thing — so it is the native <dialog> for the same reason Dialog is:
 * the focus trap, Esc and background inertness are the platform's, and the
 * platform's version is the one screen readers already understand.
 *
 * `side="bottom"` is the touch answer to a centred dialog.
 */
function Drawer({
  open,
  onClose,
  title,
  description,
  footer,
  side = "end",
  width,
  className,
  children,
  ...rest
}) {
  const ref = React.useRef(null);
  const uid = React.useId();
  const titleId = title ? uid + "-t" : undefined;
  const descId = description ? uid + "-d" : undefined;
  // A unitless length makes the declaration invalid, which drops it silently and
  // shrink-wraps the panel — so a bare number, or a numeric string, means px.
  const w = width == null || width === "" ? null : /^\d+(\.\d+)?$/.test(String(width)) ? String(width) + "px" : String(width);
  React.useEffect(() => {
    const el = ref.current;
    if (!el) return;
    if (open && !el.open) el.showModal();
    if (!open && el.open) el.close();
  }, [open]);
  return /*#__PURE__*/React.createElement("dialog", _extends({
    ref: ref,
    className: cx("lw-drawer", className),
    "data-side": side,
    style: w ? {
      "--lw-drawer-w": w
    } : undefined,
    onClose: onClose,
    onCancel: onClose,
    "aria-labelledby": titleId,
    "aria-describedby": descId
  }, rest), title && /*#__PURE__*/React.createElement("div", {
    className: "lw-drawer-head"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "lw-drawer-title",
    id: titleId
  }, title), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Close",
    title: "Close",
    onClick: onClose
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 17
  }))), /*#__PURE__*/React.createElement("div", {
    className: "lw-drawer-body"
  }, description && /*#__PURE__*/React.createElement("div", {
    id: descId
  }, description), children), footer && /*#__PURE__*/React.createElement("div", {
    className: "lw-drawer-foot"
  }, footer));
}
Object.assign(__ds_scope, { Drawer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Drawer.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Menu.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");
const isRow = it => !it.type || it.type === "item";

/**
 * The action menu. Built ON Popover rather than beside it, so there is one
 * floating surface in the system and not two that drift a radius apart.
 *
 * Keyboard is the whole point of a menu component: arrows move focus, Home/End
 * jump, typing a letter jumps to the next row starting with it, Esc closes and
 * returns focus to the trigger. Focus MOVES with the highlight — a menu that
 * paints a highlight while focus stays on the trigger tells a screen reader
 * nothing has changed.
 */
function Menu({
  items = [],
  trigger,
  onSelect,
  label,
  placement = "bottom-start",
  matchWidth,
  className,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  const listEl = React.useRef(null);
  const typed = React.useRef({
    s: "",
    t: 0
  });
  // The open INTENT lives on the component, not on a DOM node: Popover renders
  // {open && children}, so .lw-menu does not exist at the moment the trigger is
  // keyed and any flag written to it is written to null. 0 = pointer (do not move
  // focus), 1 = first row, -1 = last row.
  const intent = React.useRef(0);
  const ROWS = '[role^="menuitem"]:not([aria-disabled="true"])';
  const rows = () => Array.from(listEl.current ? listEl.current.querySelectorAll(ROWS) : []);
  const focusAt = i => {
    const r = rows();
    // An empty list here means the panel is not mounted, not that the menu has no
    // rows. Returning silently is what hid a broken open-focus through two rounds.
    if (!r.length) {
      if (typeof console !== "undefined") console.warn("Menu: no focusable rows — the list is not mounted.");
      return;
    }
    const el = r[(i + r.length) % r.length];
    el && el.focus({
      preventScroll: true
    });
  };

  // Focus on open is driven by the list's own CALLBACK REF, not by an effect:
  // the effect ordering between this component and Popover is not something to
  // depend on, and it was wrong. The callback fires the moment .lw-menu attaches.
  //
  // The focus itself is deferred one microtask because at ref-attach time the
  // panel is still display:none — Popover calls showPopover() in its own effect,
  // which has not run yet, and focus() on a display:none element is a no-op. A
  // microtask lands after the whole commit, effects included, and unlike
  // requestAnimationFrame it still runs in a hidden or throttled document.
  const listRef = React.useCallback(el => {
    listEl.current = el;
    if (!el) return;
    const want = intent.current;
    intent.current = 0;
    if (!want) return;
    queueMicrotask(() => {
      const r = Array.from(el.querySelectorAll(ROWS));
      const target = want === 1 ? r[0] : r[r.length - 1];
      if (target) target.focus({
        preventScroll: true
      });
    });
  }, []);

  // Opening with a keyboard must land ON the first row. Opening with a pointer
  // must NOT — moving focus off the trigger on a mouse click is what makes a
  // menu feel like it stole the page.
  // A menu closed by Escape or an outside click must not carry a stale intent
  // into the next pointer-driven open.
  React.useEffect(() => {
    if (!open) intent.current = 0;
  }, [open]);
  const onKeyDown = e => {
    const r = rows();
    const i = r.indexOf(document.activeElement);
    if (e.key === "ArrowDown") {
      e.preventDefault();
      return focusAt(i + 1);
    }
    if (e.key === "ArrowUp") {
      e.preventDefault();
      return focusAt(i < 0 ? -1 : i - 1);
    }
    if (e.key === "Home") {
      e.preventDefault();
      return focusAt(0);
    }
    if (e.key === "End") {
      e.preventDefault();
      return focusAt(r.length - 1);
    }
    if (e.key === "Tab") {
      setOpen(false);
      return;
    }
    // Typeahead. A menu long enough to need arrows is long enough that a user
    // will try a letter, and nothing happening reads as broken.
    if (e.key.length === 1 && !e.metaKey && !e.ctrlKey && !e.altKey) {
      const now = Date.now();
      typed.current.s = now - typed.current.t > 700 ? e.key : typed.current.s + e.key;
      typed.current.t = now;
      const q = typed.current.s.toLowerCase();
      const hit = r.findIndex((el, n) => n > i && (el.textContent || "").trim().toLowerCase().startsWith(q));
      const from0 = r.findIndex(el => (el.textContent || "").trim().toLowerCase().startsWith(q));
      const target = hit >= 0 ? hit : from0;
      if (target >= 0) {
        e.preventDefault();
        focusAt(target);
      }
    }
  };
  const choose = it => {
    if (it.disabled) return;
    setOpen(false);
    it.onSelect ? it.onSelect(it) : onSelect && onSelect(it.value, it);
  };
  const triggerEl = React.isValidElement(trigger) ? React.cloneElement(trigger, {
    onKeyDown: e => {
      trigger.props.onKeyDown && trigger.props.onKeyDown(e);
      if (e.defaultPrevented) return;
      // The menu-button pattern: Down/Enter/Space open onto the FIRST row, Up
      // onto the LAST. Down and Up also have to open the menu themselves —
      // they never reach the trigger's click handler.
      if (e.key === "ArrowDown" || e.key === "Enter" || e.key === " ") {
        intent.current = 1;
        if (e.key === "ArrowDown") {
          e.preventDefault();
          setOpen(true);
        }
      } else if (e.key === "ArrowUp") {
        intent.current = -1;
        e.preventDefault();
        setOpen(true);
      }
    }
  }) : trigger;
  return /*#__PURE__*/React.createElement(__ds_scope.Popover, _extends({
    trigger: triggerEl,
    open: open,
    onOpenChange: setOpen,
    role: "menu",
    label: label,
    placement: placement,
    matchWidth: matchWidth
  }, rest), /*#__PURE__*/React.createElement("div", {
    ref: listRef,
    className: cx("lw-menu", className),
    onKeyDown: onKeyDown
  }, items.map((it, i) => {
    if (it.type === "separator") return /*#__PURE__*/React.createElement("hr", {
      key: i,
      className: "lw-menu-sep"
    });
    if (it.type === "label") return /*#__PURE__*/React.createElement("div", {
      key: i,
      className: "lw-menu-label"
    }, it.label);
    const checkable = it.checked != null;
    const Tag = it.href ? "a" : "button";
    return /*#__PURE__*/React.createElement(Tag, {
      key: it.value ?? i,
      className: cx("lw-menu-item", it.danger && "danger"),
      type: it.href ? undefined : "button",
      href: it.href || undefined,
      role: checkable ? "menuitemcheckbox" : "menuitem",
      "aria-checked": checkable ? !!it.checked : undefined,
      "aria-disabled": it.disabled ? "true" : undefined,
      "data-checked": checkable && it.checked ? "true" : undefined,
      tabIndex: -1,
      onClick: e => {
        if (!it.href) e.preventDefault();
        choose(it);
      }
    }, (checkable || it.icon) && /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-lead"
    }, checkable ? it.checked ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "checkmark",
      size: 14
    }) : null : /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: it.icon,
      size: 15
    })), /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-text"
    }, it.label), it.kbd && /*#__PURE__*/React.createElement("span", {
      className: "lw-menu-kbd"
    }, it.kbd));
  })));
}
Object.assign(__ds_scope, { Menu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Menu.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Toast.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** Status by tint AND a mono label — never tint alone. The region is a live
 *  region so an announcement is not silent for a screen reader.
 *
 *  Pass `onClose` for a dismiss control. It is opt-in: an auto-dismissing toast
 *  that also carries an X invites the user to race the timer. */
function Toast({
  tone = "info",
  label,
  onClose,
  children,
  className,
  ...rest
}) {
  const k = label || (tone === "ok" ? "done" : tone === "warn" ? "warn" : tone === "err" ? "error" : "info");
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-toast", tone !== "info" && tone, className),
    role: tone === "err" ? "alert" : "status"
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "k"
  }, k), /*#__PURE__*/React.createElement("span", {
    className: "msg"
  }, children), onClose && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-icon-btn",
    "aria-label": "Dismiss",
    title: "Dismiss",
    onClick: onClose
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 15
  })));
}
function ToastRegion({
  className,
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-toast-region", className),
    role: "region",
    "aria-live": "polite",
    "aria-label": "Notifications"
  }, rest), children);
}
Object.assign(__ds_scope, { Toast, ToastRegion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Toast.jsx", error: String((e && e.message) || e) }); }

// components/primitives/Skeleton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/** A placeholder shaped like the thing it replaces. Give it the real element's
 *  dimensions, or the page will jump when the content lands. */
function Skeleton({
  shape = "block",
  width,
  height,
  lines,
  className,
  style,
  ...rest
}) {
  if (lines) {
    return /*#__PURE__*/React.createElement("span", _extends({
      className: "lw-stack-4",
      style: {
        display: "flex",
        flexDirection: "column",
        ...style
      },
      "aria-hidden": "true"
    }, rest), Array.from({
      length: lines
    }, (_, i) => /*#__PURE__*/React.createElement("span", {
      key: i,
      className: "lw-skeleton text",
      style: {
        width: i === lines - 1 ? "62%" : "100%"
      }
    })));
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("lw-skeleton", shape !== "block" && shape, className),
    style: {
      width,
      height,
      ...style
    },
    "aria-hidden": "true"
  }, rest));
}
Object.assign(__ds_scope, { Skeleton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/primitives/Skeleton.jsx", error: String((e && e.message) || e) }); }

// components/data/StateView.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const cx = (...a) => a.filter(Boolean).join(" ");

/* Five states, one set. Shipping only the empty one is how the other four get
   invented per product, each with its own tone and its own idea of whether
   there is an action. The defaults below are the house answers. */
const PRESETS = {
  empty: {
    icon: "inbox",
    title: "Nothing here yet"
  },
  loading: {
    icon: null,
    title: "Loading…"
  },
  error: {
    icon: "x-circle",
    title: "Something went wrong",
    description: "The request failed. Nothing was changed.",
    actionLabel: "Try again"
  },
  offline: {
    icon: "webhook",
    title: "You are offline",
    description: "Reconnect to load this. Anything you have typed is kept.",
    actionLabel: "Retry"
  },
  denied: {
    icon: "lock",
    title: "You do not have access",
    description: "Ask a workspace admin for permission to view this."
  }
};

/**
 * Empty, loading, error, offline and no-permission — one component, because
 * they are one set, and a product that ships only `empty` invents the other
 * four screen by screen.
 *
 * Exactly one action, like `EmptyState`: a dead end with three buttons is a
 * dead end with three ways to guess.
 *
 * `error` and `offline` are announced with `role="alert"`; `loading` uses
 * `role="status"` and `aria-busy` — an interruption and a progress report are
 * not the same announcement.
 */
function StateView({
  variant = "empty",
  icon,
  title,
  description,
  action,
  actionLabel,
  onAction,
  lines = 3,
  className,
  children,
  ...rest
}) {
  const p = PRESETS[variant] || PRESETS.empty;
  if (variant === "loading") {
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cx("lw-state", className),
      "data-variant": "loading",
      role: "status",
      "aria-busy": "true",
      style: {
        justifyItems: "stretch",
        textAlign: "left",
        gap: "var(--lw-space-12)"
      }
    }, rest), /*#__PURE__*/React.createElement("span", {
      className: "lw-sr-only"
    }, title || p.title), /*#__PURE__*/React.createElement(__ds_scope.Skeleton, {
      lines: lines
    }));
  }
  const isAlert = variant === "error" || variant === "offline";
  const label = actionLabel || p.actionLabel;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("lw-state", className),
    "data-variant": variant,
    role: isAlert ? "alert" : undefined
  }, rest), (icon || p.icon) && /*#__PURE__*/React.createElement("span", {
    className: "lw-state-ic"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon || p.icon,
    size: 20
  })), /*#__PURE__*/React.createElement("span", {
    className: "lw-state-title"
  }, title || p.title), (description || p.description) && /*#__PURE__*/React.createElement("p", {
    className: "lw-state-desc"
  }, description || p.description), children, (action || label && onAction) && /*#__PURE__*/React.createElement("div", {
    className: "lw-state-actions"
  }, action || /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "lw-btn lw-btn-sm",
    onClick: onAction
  }, label)));
}
Object.assign(__ds_scope, { StateView });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StateView.jsx", error: String((e && e.message) || e) }); }

// hooks.js
try { (() => {
/**
 * Runtime hooks — @leanwise/design/hooks
 *
 * Every hook here exists because the same 20 lines were being rewritten per app
 * with a different bug each time. They are all SSR-safe (no window at module
 * scope, no state read during render) and all honour prefers-reduced-motion by
 * completing instantly rather than by doing nothing — a reader who prefers less
 * motion still needs to see the final state.
 */
const {
  useState,
  useEffect,
  useRef,
  useCallback
} = React;
const canDOM = () => typeof window !== "undefined";
const prefersReduced = () => canDOM() && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

/* ---- theme ---------------------------------------------------------------- */

const THEME_KEY = "lw-theme"; /* the key ThemeToggle already writes — the hook
                                 and the component must never disagree */
const read = () => {
  try {
    return localStorage.getItem(THEME_KEY) || "system";
  } catch (e) {
    return "system";
  }
};
const systemDark = () => canDOM() && window.matchMedia("(prefers-color-scheme: dark)").matches;
function paint(mode) {
  const dark = mode === "dark" || mode === "system" && systemDark();
  const el = document.documentElement;
  el.classList.toggle("dark", dark);
  el.setAttribute("data-theme", dark ? "dark" : "light");
  return dark;
}

/**
 * Three states, not two: light / dark / system. Returns the user's CHOICE and
 * the RESOLVED scheme separately, because a component that needs to pick a tier
 * (brandVars) wants the resolved one while the picker UI wants the choice.
 *
 *   const { mode, resolved, setMode } = useTheme();
 */
function useTheme() {
  const [mode, setMode] = useState("system");
  const [resolved, setResolved] = useState("light");

  /* Mount, not render: the server has no localStorage and guessing here is what
     produces the one-frame flash of the wrong theme. */
  useEffect(() => {
    const m = read();
    setMode(m);
    setResolved(paint(m) ? "dark" : "light");
  }, []);

  /* While the choice is "system", the OS is still in charge. */
  useEffect(() => {
    if (mode !== "system" || !canDOM()) return;
    const mq = window.matchMedia("(prefers-color-scheme: dark)");
    const on = () => setResolved(paint("system") ? "dark" : "light");
    mq.addEventListener("change", on);
    return () => mq.removeEventListener("change", on);
  }, [mode]);
  const choose = useCallback(m => {
    setMode(m);
    try {
      localStorage.setItem(THEME_KEY, m);
    } catch (e) {}
    setResolved(paint(m) ? "dark" : "light");
  }, []);
  return {
    mode,
    resolved,
    setMode: choose,
    isDark: resolved === "dark"
  };
}

/* ---- reveal --------------------------------------------------------------- */

/**
 * One-shot enter reveal. Returns a ref and a boolean; you own the CSS.
 * Unobserves on first hit — a reveal that re-fires on scroll-up is a distraction,
 * not an animation. Under reduced motion it reports true immediately.
 *
 *   const [ref, shown] = useReveal();
 *   <div ref={ref} data-shown={shown} />
 */
function useReveal({
  threshold = 0.15,
  rootMargin = "0px 0px -10% 0px"
} = {}) {
  const ref = useRef(null);
  const [shown, setShown] = useState(false);
  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    if (prefersReduced() || typeof IntersectionObserver === "undefined") {
      setShown(true);
      return;
    }
    const io = new IntersectionObserver(([e]) => {
      if (e.isIntersecting) {
        setShown(true);
        io.unobserve(el);
      }
    }, {
      threshold,
      rootMargin
    });
    io.observe(el);
    return () => io.disconnect();
  }, [threshold, rootMargin]);
  return [ref, shown];
}

/* ---- spotlight ------------------------------------------------------------ */

/**
 * Writes --lw-mx / --lw-my (0-100%) on the element so CSS can place a
 * pointer-following highlight. Coordinates go to the DOM, not to React state:
 * a setState per mousemove re-renders the subtree 60 times a second.
 * Off entirely on a coarse pointer and under reduced motion.
 */
function useSpotlight() {
  const ref = useRef(null);
  useEffect(() => {
    const el = ref.current;
    if (!el || prefersReduced()) return;
    if (window.matchMedia("(pointer: coarse)").matches) return;
    let frame = 0;
    const on = e => {
      if (frame) return;
      frame = requestAnimationFrame(() => {
        frame = 0;
        const r = el.getBoundingClientRect();
        el.style.setProperty("--lw-mx", ((e.clientX - r.left) / r.width * 100).toFixed(2) + "%");
        el.style.setProperty("--lw-my", ((e.clientY - r.top) / r.height * 100).toFixed(2) + "%");
      });
    };
    const off = () => {
      el.style.removeProperty("--lw-mx");
      el.style.removeProperty("--lw-my");
    };
    el.addEventListener("pointermove", on);
    el.addEventListener("pointerleave", off);
    return () => {
      cancelAnimationFrame(frame);
      el.removeEventListener("pointermove", on);
      el.removeEventListener("pointerleave", off);
    };
  }, []);
  return ref;
}

/* ---- cascade -------------------------------------------------------------- */

/**
 * Stagger delays for a list — deterministic, so the third card's delay is the
 * same on every render and in every screenshot test. Returns a function, not an
 * array, so the list length can change without re-running a hook.
 *
 *   const delay = useDeterministicCascade({ step: 60, max: 8 });
 *   items.map((it, i) => <Card style={{ transitionDelay: delay(i) }} />)
 */
function useDeterministicCascade({
  step = 60,
  max = 8,
  base = 0
} = {}) {
  const reduced = useReducedMotion();
  return useCallback(i => reduced ? "0ms" : `${base + Math.min(i, max) * step}ms`, [step, max, base, reduced]);
}

/** Live prefers-reduced-motion. Re-renders when the OS setting changes. */
function useReducedMotion() {
  const [reduced, setReduced] = useState(false);
  useEffect(() => {
    if (!canDOM()) return;
    const mq = window.matchMedia("(prefers-reduced-motion: reduce)");
    setReduced(mq.matches);
    const on = () => setReduced(mq.matches);
    mq.addEventListener("change", on);
    return () => mq.removeEventListener("change", on);
  }, []);
  return reduced;
}

/* ---- counter -------------------------------------------------------------- */

/**
 * Counts a KPI up to its value. NOT a hook — call it on an element, imperatively,
 * from a reveal. Writes textContent directly for the same reason as useSpotlight.
 * Under reduced motion it sets the final number and returns.
 *
 *   animateCounter(el, 1284, { format: (n) => n.toLocaleString() })
 *
 * Returns a cancel function.
 */
function animateCounter(el, to, {
  from = 0,
  duration = 900,
  decimals = 0,
  format
} = {}) {
  if (!el) return () => {};
  const fmt = format || (n => n.toFixed(decimals));
  if (prefersReduced()) {
    el.textContent = fmt(to);
    return () => {};
  }
  /* The house curve, matched to --lw-ease-out so a counter and the card it sits
     in settle together. */
  const ease = t => 1 - (1 - t) ** 3;
  const t0 = performance.now();
  let raf = 0,
    done = false;
  const tick = now => {
    const t = Math.min(1, (now - t0) / duration);
    el.textContent = fmt(from + (to - from) * ease(t));
    if (t < 1) raf = requestAnimationFrame(tick);else done = true;
  };
  raf = requestAnimationFrame(tick);
  return () => {
    if (!done) {
      cancelAnimationFrame(raf);
      el.textContent = fmt(to);
    }
  };
}
Object.assign(__ds_scope, { useTheme, useReveal, useSpotlight, useDeterministicCascade, useReducedMotion, animateCounter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "hooks.js", error: String((e && e.message) || e) }); }

// react.js
try { (() => {

Object.assign(__ds_scope, { Avatar: __ds_scope.Avatar, Button: __ds_scope.Button, Card: __ds_scope.Card, CardHead: __ds_scope.CardHead, CardTitle: __ds_scope.CardTitle, CardBody: __ds_scope.CardBody, CardFoot: __ds_scope.CardFoot, Chip: __ds_scope.Chip, Eyebrow: __ds_scope.Eyebrow, Skeleton: __ds_scope.Skeleton, Icon: __ds_scope.Icon, iconNames: __ds_scope.iconNames, Page: __ds_scope.Page, Container: __ds_scope.Container, Stack: __ds_scope.Stack, Cluster: __ds_scope.Cluster, Grid: __ds_scope.Grid, Split: __ds_scope.Split, Section: __ds_scope.Section, Field: __ds_scope.Field, Input: __ds_scope.Input, InputGroup: __ds_scope.InputGroup, Textarea: __ds_scope.Textarea, Select: __ds_scope.Select, Switch: __ds_scope.Switch, Checkbox: __ds_scope.Checkbox, Segmented: __ds_scope.Segmented, Combobox: __ds_scope.Combobox, Calendar: __ds_scope.Calendar, DatePicker: __ds_scope.DatePicker, RANGE_PRESETS: __ds_scope.RANGE_PRESETS, FileUpload: __ds_scope.FileUpload, formatBytes: __ds_scope.formatBytes, Stepper: __ds_scope.Stepper, RichText: __ds_scope.RichText, Table: __ds_scope.Table, KpiTile: __ds_scope.KpiTile, StatMeter: __ds_scope.StatMeter, EmptyState: __ds_scope.EmptyState, StateView: __ds_scope.StateView, Console: __ds_scope.Console, CodeBlock: __ds_scope.CodeBlock, Pagination: __ds_scope.Pagination, DataGrid: __ds_scope.DataGrid, Progress: __ds_scope.Progress, FilterBar: __ds_scope.FilterBar, Toolbar: __ds_scope.Toolbar, BarChart: __ds_scope.BarChart, LineChart: __ds_scope.LineChart, ActivityFeed: __ds_scope.ActivityFeed, timeAgo: __ds_scope.timeAgo, TopBar: __ds_scope.TopBar, AppBar: __ds_scope.AppBar, Sidebar: __ds_scope.Sidebar, NavItem: __ds_scope.NavItem, Tabs: __ds_scope.Tabs, Breadcrumbs: __ds_scope.Breadcrumbs, ThemeToggle: __ds_scope.ThemeToggle, CommandPalette: __ds_scope.CommandPalette, score: __ds_scope.score, BottomNav: __ds_scope.BottomNav, Dialog: __ds_scope.Dialog, Toast: __ds_scope.Toast, ToastRegion: __ds_scope.ToastRegion, Tooltip: __ds_scope.Tooltip, Popover: __ds_scope.Popover, Menu: __ds_scope.Menu, Drawer: __ds_scope.Drawer, PromptInput: __ds_scope.PromptInput, Message: __ds_scope.Message, SourceChip: __ds_scope.SourceChip, SourceList: __ds_scope.SourceList, ConfidenceMeter: __ds_scope.ConfidenceMeter, AgentTrace: __ds_scope.AgentTrace, ToolCall: __ds_scope.ToolCall, DiffReview: __ds_scope.DiffReview, Artifact: __ds_scope.Artifact, Feedback: __ds_scope.Feedback, Hero: __ds_scope.Hero, FeatureGrid: __ds_scope.FeatureGrid, StoryCard: __ds_scope.StoryCard, LogoRail: __ds_scope.LogoRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "react.js", error: String((e && e.message) || e) }); }

// tailwind-preset.cjs
try { (() => {
/**
 * LeanWise Design System — Tailwind v3 preset.
 *
 *   // tailwind.config.js
 *   export default {
 *     presets: [require("@leanwise/design/tailwind-preset")],   // resolves to tailwind-preset.cjs
 *     content: ["./index.html", "./src/**\/*.{ts,tsx}"],
 *   }
 *
 * The point of registering cta/success/warning/brand here (and not just in CSS)
 * is that it makes `bg-cta` and `text-success` REAL utilities. Without them, a dev
 * reaches for `bg-[hsl(var(--cta))]` — an arbitrary-value escape that bypasses the
 * design system and cannot be linted. VSS had eleven of those. Give people the
 * utility they want and the escape hatch stops being tempting.
 *
 * Every color resolves through `hsl(var(--x) / <alpha-value>)`, so `bg-primary/15`
 * works — which is what badges and chips need.
 */

/* .cjs, not .js — package.json declares "type": "module" for react.js / brand.js /
   hooks.js, which would make a .js file here ESM: a CJS tailwind.config.js doing
   require() of it throws ERR_REQUIRE_ESM, and `module.exports` inside ESM exports
   nothing at all. The extension is what pins this one file to CommonJS.

   The guard below stays for browser-side tooling that reads this file without a
   module wrapper: in Node the `var` redeclares the wrapper's own parameter without
   touching it, so the real module object survives; in a browser it hoists to
   undefined and gets a shim. */
if (typeof module === "undefined") {
  var module = {
    exports: {}
  };
}

/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class", '[data-theme="dark"]'],
  theme: {
    container: {
      center: true,
      padding: "var(--lw-space-16)",
      screens: {
        "2xl": "var(--lw-col-page)"
      }
    },
    extend: {
      colors: {
        border: "hsl(var(--border) / <alpha-value>)",
        input: "hsl(var(--input) / <alpha-value>)",
        ring: "hsl(var(--ring) / <alpha-value>)",
        background: "hsl(var(--background) / <alpha-value>)",
        foreground: "hsl(var(--foreground) / <alpha-value>)",
        primary: {
          DEFAULT: "hsl(var(--primary) / <alpha-value>)",
          foreground: "hsl(var(--primary-foreground) / <alpha-value>)"
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary) / <alpha-value>)",
          foreground: "hsl(var(--secondary-foreground) / <alpha-value>)"
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive) / <alpha-value>)",
          foreground: "hsl(var(--destructive-foreground) / <alpha-value>)",
          soft: "hsl(var(--danger-soft) / <alpha-value>)",
          on: "hsl(var(--danger-on) / <alpha-value>)"
        },
        muted: {
          DEFAULT: "hsl(var(--muted) / <alpha-value>)",
          foreground: "hsl(var(--muted-foreground) / <alpha-value>)"
        },
        accent: {
          DEFAULT: "hsl(var(--accent) / <alpha-value>)",
          foreground: "hsl(var(--accent-foreground) / <alpha-value>)"
        },
        popover: {
          DEFAULT: "hsl(var(--popover) / <alpha-value>)",
          foreground: "hsl(var(--popover-foreground) / <alpha-value>)"
        },
        card: {
          DEFAULT: "hsl(var(--card) / <alpha-value>)",
          foreground: "hsl(var(--card-foreground) / <alpha-value>)"
        },
        // --- LeanWise additions -------------------------------------------
        // The one high-energy action color. `variant="cta"` on Button; nowhere else.
        // The fourth status — idle / pending / draft. `bg-neutral` / `text-neutral-on`.
        neutral: {
          DEFAULT: "hsl(var(--neutral) / <alpha-value>)",
          foreground: "hsl(var(--neutral-foreground) / <alpha-value>)",
          soft: "hsl(var(--neutral-soft) / <alpha-value>)",
          on: "hsl(var(--neutral-on) / <alpha-value>)"
        },
        cta: {
          DEFAULT: "hsl(var(--cta) / <alpha-value>)",
          foreground: "hsl(var(--cta-foreground) / <alpha-value>)",
          soft: "hsl(var(--cta-soft) / <alpha-value>)",
          // `text-cta-on` — orange as TEXT. NOT `text-cta`, which is the fill and
          // scores 2.80 on white. Mirrors success/warning/danger `-on`.
          on: "hsl(var(--cta-on) / <alpha-value>)"
        },
        // Cyan as TEXT. `text-brand` for a link/active label — NOT `text-primary`,
        // which is the fill color. Since v0.8.0 the brand fill also reads on white (5.68),
        // but the role token stays because DARK re-points it to brand-400.
        brand: "hsl(var(--brand) / <alpha-value>)",
        success: {
          DEFAULT: "hsl(var(--success) / <alpha-value>)",
          foreground: "hsl(var(--success-foreground) / <alpha-value>)",
          soft: "hsl(var(--success-soft) / <alpha-value>)",
          on: "hsl(var(--success-on) / <alpha-value>)"
        },
        warning: {
          DEFAULT: "hsl(var(--warning) / <alpha-value>)",
          foreground: "hsl(var(--warning-foreground) / <alpha-value>)",
          soft: "hsl(var(--warning-soft) / <alpha-value>)",
          on: "hsl(var(--warning-on) / <alpha-value>)"
        },
        navy: "hsl(var(--navy) / <alpha-value>)"
      },
      borderRadius: {
        sm: "var(--lw-radius-sm)",
        DEFAULT: "var(--lw-radius-md)",
        md: "var(--lw-radius-md)",
        lg: "var(--lw-radius-lg)",
        xl: "var(--lw-radius-xl)",
        "2xl": "var(--lw-radius-2xl)"
      },
      fontFamily: {
        sans: ["Geist", "-apple-system", "BlinkMacSystemFont", "Segoe UI", "system-ui", "sans-serif"],
        mono: ["Geist Mono", "ui-monospace", "SFMono-Regular", "Menlo", "monospace"]
      },
      // The fluid role scale. `text-h1` is a real utility, so a heading never
      // hand-rolls a clamp().
      fontSize: {
        // Marketing/hero display — one notch above h1, weight 500 (lighter editorial
        // voice than the 600 headings). `text-display` on a landing/auth hero headline.
        display: ["var(--lw-text-display)", {
          lineHeight: "var(--lw-lh-tight)",
          letterSpacing: "var(--lw-tracking-tighter)",
          fontWeight: "500"
        }],
        h1: ["var(--lw-text-h1)", {
          lineHeight: "var(--lw-lh-tight)",
          letterSpacing: "var(--lw-tracking-tighter)",
          fontWeight: "600"
        }],
        h2: ["var(--lw-text-h2)", {
          lineHeight: "var(--lw-lh-snug)",
          letterSpacing: "var(--lw-tracking-tight)",
          fontWeight: "600"
        }],
        h3: ["var(--lw-text-h3)", {
          lineHeight: "1.18",
          letterSpacing: "var(--lw-tracking-tight)",
          fontWeight: "600"
        }],
        lead: ["var(--lw-text-lead)", {
          lineHeight: "1.6"
        }],
        eyebrow: ["var(--lw-text-eyebrow)", {
          letterSpacing: "var(--lw-tracking-eyebrow)",
          fontWeight: "500"
        }]
      },
      boxShadow: {
        sm: "var(--lw-shadow-sm)",
        DEFAULT: "var(--lw-shadow-md)",
        md: "var(--lw-shadow-md)",
        lg: "var(--lw-shadow-lg)",
        focus: "var(--lw-focus-ring)"
      },
      transitionTimingFunction: {
        out: "var(--lw-ease-out)"
      },
      transitionDuration: {
        fast: "var(--lw-duration-fast)",
        DEFAULT: "var(--lw-duration)",
        slow: "var(--lw-duration-slow)"
      },
      backgroundImage: {
        // The hero/brand panel gradient (navy → cyan, following the mark).
        "gradient-brand": "var(--lw-gradient-brand)",
        // The soft radial brand glow behind auth cards and empty hero states. Registered
        // as a real utility so nobody has to reach for an arbitrary value to get it —
        // that escape hatch is exactly how VSS's auth card ended up keyed to --accent
        // (a hover surface) instead of the brand, rendering an invisible grey wash.
        "brand-wash": "radial-gradient(40rem at 50% -10%, hsl(var(--primary) / 0.18), transparent)",
        // The editorial auth/landing aside: two cyan radial washes over the base. Keyed to
        // --primary so it themes per tenant. `bg-hero-aside` — a real utility, no arbitrary
        // value, so it stays lintable (the escape hatch is exactly the --accent footgun above).
        "hero-aside": "radial-gradient(60rem at 12% 8%, hsl(var(--primary) / 0.16), transparent 55%), radial-gradient(48rem at 88% 96%, hsl(var(--primary) / 0.12), transparent 55%)",
        // A faint hairline lattice for texture over the aside, keyed to the border token so
        // it disappears correctly in dark mode. Layer as `bg-hero-aside bg-hairline-grid`.
        "hairline-grid": "repeating-linear-gradient(0deg, hsl(var(--border) / 0.55) 0 1px, transparent 1px 88px), repeating-linear-gradient(90deg, hsl(var(--border) / 0.55) 0 1px, transparent 1px 88px)"
      },
      keyframes: {
        "accordion-down": {
          from: {
            height: "0"
          },
          to: {
            height: "var(--radix-accordion-content-height)"
          }
        },
        "accordion-up": {
          from: {
            height: "var(--radix-accordion-content-height)"
          },
          to: {
            height: "0"
          }
        },
        // Editorial entrance motion. `fade-up` is the signature hero/message reveal (rise +
        // fade); the two others are calmer variants for panels and cards. The global
        // prefers-reduced-motion block in tokens.css neutralizes all of them — no per-use guard.
        "fade-up": {
          from: {
            opacity: "0",
            transform: "translateY(10px)"
          },
          to: {
            opacity: "1",
            transform: "none"
          }
        },
        "fade-in": {
          from: {
            opacity: "0"
          },
          to: {
            opacity: "1"
          }
        },
        "scale-in": {
          from: {
            opacity: "0",
            transform: "scale(0.97) translateY(6px)"
          },
          to: {
            opacity: "1",
            transform: "none"
          }
        },
        // Logo-rail loop: duplicate the track once in markup so -100% is seamless.
        // Neutralized by the global prefers-reduced-motion block in tokens.css.
        marquee: {
          from: {
            transform: "translateX(0)"
          },
          to: {
            transform: "translateX(-100%)"
          }
        }
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
        // `animate-rise` is the one to reach for on a hero block or a chat message.
        rise: "fade-up var(--lw-duration-slow) var(--lw-ease-out) both",
        "fade-in": "fade-in var(--lw-duration-slow) var(--lw-ease-out) both",
        "scale-in": "scale-in var(--lw-duration-slow) var(--lw-ease-out) both",
        marquee: "marquee 40s linear infinite"
      }
    }
  }
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "tailwind-preset.cjs", error: String((e && e.message) || e) }); }

__ds_ns.AgentTrace = __ds_scope.AgentTrace;

__ds_ns.Artifact = __ds_scope.Artifact;

__ds_ns.ConfidenceMeter = __ds_scope.ConfidenceMeter;

__ds_ns.DiffReview = __ds_scope.DiffReview;

__ds_ns.Feedback = __ds_scope.Feedback;

__ds_ns.Message = __ds_scope.Message;

__ds_ns.PromptInput = __ds_scope.PromptInput;

__ds_ns.SourceChip = __ds_scope.SourceChip;

__ds_ns.SourceList = __ds_scope.SourceList;

__ds_ns.ToolCall = __ds_scope.ToolCall;

__ds_ns.ActivityFeed = __ds_scope.ActivityFeed;

__ds_ns.BarChart = __ds_scope.BarChart;

__ds_ns.CodeBlock = __ds_scope.CodeBlock;

__ds_ns.Console = __ds_scope.Console;

__ds_ns.DataGrid = __ds_scope.DataGrid;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.FilterBar = __ds_scope.FilterBar;

__ds_ns.Toolbar = __ds_scope.Toolbar;

__ds_ns.KpiTile = __ds_scope.KpiTile;

__ds_ns.LineChart = __ds_scope.LineChart;

__ds_ns.Pagination = __ds_scope.Pagination;

__ds_ns.Progress = __ds_scope.Progress;

__ds_ns.StatMeter = __ds_scope.StatMeter;

__ds_ns.StateView = __ds_scope.StateView;

__ds_ns.Table = __ds_scope.Table;

__ds_ns.SERIES = __ds_scope.SERIES;

__ds_ns.DataTable = __ds_scope.DataTable;

__ds_ns.Legend = __ds_scope.Legend;

__ds_ns.Calendar = __ds_scope.Calendar;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Combobox = __ds_scope.Combobox;

__ds_ns.RANGE_PRESETS = __ds_scope.RANGE_PRESETS;

__ds_ns.DatePicker = __ds_scope.DatePicker;

__ds_ns.Field = __ds_scope.Field;

__ds_ns.FileUpload = __ds_scope.FileUpload;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.InputGroup = __ds_scope.InputGroup;

__ds_ns.RichText = __ds_scope.RichText;

__ds_ns.Segmented = __ds_scope.Segmented;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Stepper = __ds_scope.Stepper;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Textarea = __ds_scope.Textarea;

__ds_ns.Cluster = __ds_scope.Cluster;

__ds_ns.Container = __ds_scope.Container;

__ds_ns.Grid = __ds_scope.Grid;

__ds_ns.Page = __ds_scope.Page;

__ds_ns.Section = __ds_scope.Section;

__ds_ns.Split = __ds_scope.Split;

__ds_ns.Stack = __ds_scope.Stack;

__ds_ns.FeatureGrid = __ds_scope.FeatureGrid;

__ds_ns.Hero = __ds_scope.Hero;

__ds_ns.LogoRail = __ds_scope.LogoRail;

__ds_ns.StoryCard = __ds_scope.StoryCard;

__ds_ns.AppBar = __ds_scope.AppBar;

__ds_ns.BottomNav = __ds_scope.BottomNav;

__ds_ns.Breadcrumbs = __ds_scope.Breadcrumbs;

__ds_ns.CommandPalette = __ds_scope.CommandPalette;

__ds_ns.Sidebar = __ds_scope.Sidebar;

__ds_ns.NavItem = __ds_scope.NavItem;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.ThemeToggle = __ds_scope.ThemeToggle;

__ds_ns.TopBar = __ds_scope.TopBar;

__ds_ns.Dialog = __ds_scope.Dialog;

__ds_ns.Drawer = __ds_scope.Drawer;

__ds_ns.Menu = __ds_scope.Menu;

__ds_ns.Popover = __ds_scope.Popover;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.ToastRegion = __ds_scope.ToastRegion;

__ds_ns.Tooltip = __ds_scope.Tooltip;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Card = __ds_scope.Card;

__ds_ns.CardHead = __ds_scope.CardHead;

__ds_ns.CardTitle = __ds_scope.CardTitle;

__ds_ns.CardBody = __ds_scope.CardBody;

__ds_ns.CardFoot = __ds_scope.CardFoot;

__ds_ns.Chip = __ds_scope.Chip;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.IconNames = __ds_scope.IconNames;

__ds_ns.Skeleton = __ds_scope.Skeleton;

})();
